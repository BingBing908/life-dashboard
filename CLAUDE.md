# life-dashboard 项目说明

Rosie 的个人工具：一个模块化仪表盘应用。工作日在电脑/网页用，周末计划放在 iPad。中文交流。

> 📄 **换对话/换窗口时先读 [PRODUCT.md](PRODUCT.md)**（2026-07-29 加，Rosie 要的「用来压缩上下文的产品文档全揽」）——
> 约 20 KB、一次读完就有全貌：产品定位、五个模块各自解决什么问题、每个设计决定的理由、
> 文件地图、数据表清单、铁律速查、当前进展。
> **本文档（80 KB）是「怎么改 / 别踩哪个坑」，按需查那一段，不用通读。**
> 两份的分工：**结论和理由**进 PRODUCT.md，**实现细节和踩坑**进这里；
> 改动如果推翻了某个产品决策（像 2026-07-29 那两条复习规则），**两份都要改**。

## 技术栈

- **桌面端**：Tauri 2（Rust 壳）+ React 19 + TypeScript + Vite
- **UI**：Tailwind CSS v4 + shadcn/ui（**Base UI 版本**）
  - 组件在 `src/components/ui/`
  - ⚠️ trigger 用 `render` prop，不是 `asChild`
  - ⚠️ `<SelectValue>` 不会自动显示选中项文字，要传 render 函数：`<SelectValue>{(v) => 名字映射}</SelectValue>`，否则显示原始 value
  - 主题变量在 `src/index.css`：蓝白管理后台配色（参考 NOC 工具）；字体幼圆（`YouYuan`，非 Windows 设备回退）
  - ⚠️ **粗体保持幼圆、不要黑体**（2026-07-27 Rosie 要求）：`index.css` 里 `strong,b,.font-semibold,.font-bold` 的 `font-family` 用 `var(--font-sans)`（幼圆优先，浏览器合成加粗），**不能**再回退到 Geist/微软雅黑那种黑体（之前那样做过，Rosie 说"不要黑体"）
- **数据**：SQLite，运行环境自动检测（`src/lib/db.ts`）：
  - Tauri 内 → tauri-plugin-sql，本地文件 `portwritingtool.db`，迁移在 `src-tauri/src/lib.rs`（`migrations()`，目前到 **v12**）
  - 纯浏览器 → sql.js (WASM) + localStorage（key `pwt-sqljs-db`），建表 SQL 在 `src/lib/schema.ts`
  - ⚠️ **改表结构必须四处同步**：`lib.rs` 加新 Migration + `schema.ts` 的 `SCHEMA_SQL`（新库建表）+ `schema.ts` 的 `BROWSER_MIGRATIONS`（旧浏览器库补列，幂等 ALTER）+ `supabase/schema.sql`（云端 Postgres 加列，Rosie 到 Supabase SQL Editor 跑）。新增的表还要加进 `src/lib/sync.ts` 的 `TABLES`
  - SQL 占位符用 `$1..$n`；浏览器层转成编号 `?N`（不是裸 `?`，因为同一参数可能复用，如软删除 `deleted_at=$1, updated_at=$1`）

## 架构约定（重要）

- **一切功能皆模块**。每个模块 = `manifest`（id/名称/图标/卡片尺寸）+ `Card`（仪表盘摘要卡）+ `Page`（完整页面），接口在 `src/modules/types.ts`
- 新模块在 `src/modules/<名字>/` 下自包含（index.tsx + data.ts），在 `src/modules/registry.ts` 注册一行即上架
- ⚠️ **列表/卡片组件（尤其含 `<input>` 的）必须定义在模块顶层，不能嵌套在页面组件函数体内**——否则父组件每次 setState 重渲染都会给嵌套组件新的函数身份→React 卸载重挂载→输入框失焦（"只能打一个字"）。数据用 props 传入。踩过：study-plan 的 `ItemRow`/`ThreeRowCard` 曾嵌套在 Page 内，笔记输入框每敲一字就失焦，2026-07-20 提到顶层修复。
- **不要**把功能硬编码进仪表盘壳子（`src/App.tsx`、`src/components/dashboard/`）。例外：`DashboardShell` 是**今日总览**（2026-07-21），会**汇总各模块今日数据**——全局经期开关 + 今日完成度(**日日学/待办/时间轴/打卡/卡路里**，2026-07-30 按真实使用频率重排，见下「总览顶排」) + 本周时间轴完成柱 + 模块入口卡。它按只读方式 import 各模块 data（listItems/listCheckStatus/listTodos/getMeals/dayCalories/getCalTarget/listHabits/getCheckins/listAllEntries/entryDone）；这是"总览"的合理跨模块读取，不是把功能塞进壳子。**新增模块若想上今日总览，在这里加一块**。
- 所有业务表带同步预留字段：`id`(UUID)、`created_at`、`updated_at`、`device_id`、`deleted_at`（软删除，查询过滤 `deleted_at IS NULL`）
- 通用组件/工具：`src/components/EditableText.tsx`（点文字就地编辑）；`src/components/Collapse.tsx`（通用折叠区 title/count/hint/right/defaultOpen，待办的「历史已完成」「今天的学练计划」等复用，重复即抽组件）；`src/components/DoneToggle.tsx`（计划项**三态**开关两按钮【已完成】【未完成】，替代方框勾：pending 两键都不亮／点【已完成】=done绿／点【未完成】=skip琥珀「今天做不了」／再点亮着的键撤销回 pending；可选 canComplete 门控【已完成】。**按钮统一放在每行/卡片最前**（今日卡片、一周列表、待办页镜像、待办本体条目四处一致）。待办本体条目也用它（含已完成区），待办每条下方带**选填**「我具体做了什么」输入，存 `plan_notes`（item_id=todo.id，与今日「工作」卡片共用同一份笔记）；`src/components/QuickAdd.tsx`（通用「加一行」输入框＝输入框+按钮，自带 state、回车/点按钮提交清空，props: placeholder/cta/onAdd(text)/variant；时间轴工作域「加工作」、计划域「加计划外」都用它；日日学加书/加电影/记一笔也走它，别再内联重写）；`src/lib/openLink.ts`（Tauri 系统浏览器 / 网页新标签，全应用共用，别再各写一份）；日期用 `src/lib/dates.ts`、周几用 study-plan/data 的 `dayNumOf`（唯一实现，别在模块里重写）。
- **页面宽度约定＝全宽 `p-6`，不加 max-w**（2026-07-28 定稿，Rosie 反馈：别老做很窄很居中、离边界太远）：模块 Page 根节点统一 `<div className="p-6">`，**跟待办、今日总览一致**——它俩一直是全宽，所以视觉基准是它俩。⚠️ **不要再加 `mx-auto max-w-6xl/7xl`**：那是 2026-07-20 为了治 max-w-3xl 过窄立的中间态规则，2026-07-28 Rosie 说时间轴/饮食/日日学/小表格「都窄窄的看起来很古怪」，已全部去掉（study-plan / study-log / mini-table 原为 max-w-6xl，supplement 原为 max-w-7xl）。内容要把宽度铺开、方框/卡片按合适大小放大，别缩成小块挤在中间。**改全宽后记得给网格加高一档断点**（如小表格磁贴 `sm:2 xl:3 2xl:4`），否则宽屏上卡片会被拉成大长条。
- **卡片规格＝全局统一，单一来源 `src/lib/ui.ts`**（2026-07-29 定稿，以**饮食页**为样板——Rosie 原话「这大小刚刚好，把全部的模块都默认这么大，然后放大缩小全部自适应」）。之前各页是散的：圆角 lg/xl 两种、padding p-2.5/p-3/p-4/p-5 四种、底色有的 `bg-card` 有的 `bg-background` 有的没有、标题有的在卡内有的飘在卡外——眼睛读不出层级，观感就是她说的「不和谐」。
  - **只有两级**：`CARD`＝`rounded-xl border bg-card p-4`（页面上的一块，**标题放卡内**）／`SUBCARD`＝`rounded-lg bg-muted px-3 py-2.5`（卡内的一小块，浅一层）。小块里的 `<Input>` 记得加 `bg-card`，否则在灰底上浮不起来。另有 `CARD_BTN`（可点卡片／磁贴的统一 hover）、`CARD_TITLE`、`PAGE`。
  - **写法**：`className={CARD}` 或 `className={cn(CARD, "额外的类")}`——**别再手写那串 class**。全局改一次大小就只动 `ui.ts` 一行，这正是 Rosie 要的「全部模块默认这么大」。
  - ✅ **2026-07-30 才真正落干净**（在此之前是**半成品**：只有 `CARD` 和 `TWO_COL` 真被用了，其余四个常量**一处都没落**，各页的小块／磁贴／标题全是手写 class ⇒ 「改 ui.ts 一行就全局生效」当时是**不成立的**，指望它的人会发现改了没反应）。这一轮的落点与两处**定义修正**：
    - `PAGE` **定义本来是错的**：原为 `space-y-6 p-6`，但**没有任何一页用这个组合**（六个页面根节点全是纯 `p-6`，纵向间距各页自己排）。所以它不是"没人用"，是**定错了没法用**。现已改成 `p-6`＝约定本身，落到 DashboardShell／todo／study-plan／study-log／mini-table／habit-checkin 六个根节点。⚠️ **饮食页刻意不套 PAGE**（仪表型、紧一档：`gap-4 px-6 py-4`）。
    - `CARD_SUB` **已删除，别加回来**：`text-sm text-muted-foreground` 全站 20+ 处，绝大多数**不是**卡副标题（加载中、空态、shadcn 的 card/dialog/table 原语）。给它起名字只会误导人以为「改 CARD_SUB 能改所有卡副标题」，实际要么漏一半、要么顺手把加载文案也改了。**通用 utility 组合不值得起名，只有真正的设计 token 才值得。**
    - `SUBCARD` 落到饮食页 4 处（原样同值，零外观变化）；`CARD_TITLE` 落到 9 处卡标题（supplement×5／todo／study-plan／study-log／mini-table 磁贴名）。⚠️ `App.tsx` 侧栏那个「个人工具」**刻意没换**——它是品牌名不是卡标题。
    - `CARD_BTN` 统一了**三套不同的可点 hover**：小表格磁贴（原「描边+阴影」）／总览顶排完成度卡（原「只有底色」）／饮食页「本周复盘」（原「描边+底色」）⇒ 现在都是「描边转主色 + 底色微亮」。**hover 改动不影响布局高度**，所以这轮改动对饮食页"太高"这个问题既不帮忙也不添乱。
    - ⚠️ **改这类替换后必查「导入了没用」**：`noUnusedLocals` 开着，替换掉某文件最后一处 `CARD` 用法却留着 import 会**直接构建失败**。这轮就踩到一次（mini-table 磁贴改用 `CARD_BTN` 后 `CARD` 成了孤儿 import），靠逐文件核对导入/使用数抓出来的——本机没 Node 时这个自检必须手动做。
  - ⚠️ **不要"统一"掉的例外**：带 accent 边框的**卡内功能面板**（默写框、作业框、经期提示框）保持 `rounded-lg` + 自己的配色——它们是卡里的东西，比卡小一号才对。study-log 里那 9 处 `rounded-lg border p-3` 就是这类，**不是漏改**。（饮食页「本周复盘」按钮曾用 `px-5 py-6` 吸收整栏剩余高度，2026-07-29 已改回 `py-3` 一行版——见下条。）
  - ⚠️ **剩余高度要给"吃得下它的东西"，不是给某个块的外框**（2026-07-29 定，饮食页来回改了三版才对）：①最早平摊给三张餐卡 → 每张卡中间空一块（Rosie：「白白的放在这里好丑555」）；②改成让「本周复盘」按钮 `flex-1` 全吃掉 → 按钮撑成一大片空白（Rosie：「跳转框长度缩短一点」）；③现在＝按钮 `shrink-0` 只占一行，剩余高度归「今天吃什么」这张卡，再 `min-h-0 flex-1` 一路传到每餐的 **textarea** 上。判据：链条末端得有个**真的能用掉高度的元素**（输入框、可滚区域、图表），否则长出来的只会是留白。
  - ⚠️⚠️ **密度按用途分成两套，别再"统一"回一种字号**（2026-07-30 定，Rosie 同一轮里提了两个**方向相反**的要求：「饮食这一页太大了，搞成刚刚合适一页放下」＋「日日学现在的文字都太小了，阅读起来对眼睛很不友好」）。结论：**仪表型页面**（总览／饮食／小表格）＝目标「一屏看完」，字小行密才装得下；**阅读型页面**（日日学的正文／作业框／精读文章）＝目标「连着看十分钟不累」，字要大一档、行距要松（她的历史长传一条上千字，14px/1.625 确实伤眼）。想用一套字号同时满足两者是不可能的。
    - 阅读型的单一来源＝`lib/ui.ts` 的 **`READ_BODY`**（`text-[15.5px] leading-[1.9]`）和 **`READ_TITLE`**（`text-base font-semibold`，原来条目标题跟正文同为 text-sm、压不出层级）。已用在 `EntryDoc` 的正文／作业框／术语释义、`ReadingCard` 的英文正文与整段中译、两处条目标题。
    - ⚠️ **只用在「内容」上，不要用在界面元件**（徽标、按钮、日期头、磁贴预览）——那些是 chrome，跟界面字号走；磁贴里的预览是 `line-clamp` 的，放大反而看得更少，所以 Landing 的预览刻意没改。
    - **饮食页的纵向节奏比别页紧一档**：根节点 `flex min-h-full flex-col gap-4 px-6 py-4`（别页是 `gap-6 p-6`）。**右栏是整页高度的瓶颈**——体重／饮品／零食／月历四块都是固定高度，不像左栏三餐的 textarea 会自己缩，所以想让这页更短只能从这四块里减。
    - ⚠️⚠️ **别用「给两栏各自 `overflow-y-auto`」去强行把这页压成一屏**（2026-07-30 试过，**当天就回滚了**）。Rosie 一眼看出来，原话「我包边没了」。两个后果：
      ① **CSS 规则**：`overflow-y` 一旦不是 `visible`，另一轴的 `visible` 会被**计算成 `auto`**——所以只写 `overflow-y-auto` 会凭空多出一条**横向**滚动条（截图里左栏底下那条）；
      ② 滚动容器在 **padding box** 处裁切，而卡片的 `rounded-xl border` 正好贴着那条边 ⇒ **描边被切掉、卡片看起来没有包边**。想保住描边就得在滚动容器内留 padding，但那会让卡片变窄、滚起来上下边缘照样被切。
      **结论：这页宁可整页滚，也不要栏内滚。** 真要压进一屏得**减内容高度**（合并饮品/零食两张卡是唯一够量的一刀，约省 96px＝一圈 p-4 + 一个标题行 + 一个 DateLine + 一个 gap），加滚动容器只是把溢出藏起来、还顺手破了卡片规格。**这条跟「别用 max-height 做上限」是同一类错误：用裁切/隐藏冒充布局。**
  - **自适应**：两栏布局用 `TWO_COL`（`lg:grid-cols-[minmax(0,1.18fr)_minmax(0,1fr)]`，窄屏塞成一栏）。⚠️ **别用内联 `style={{gridTemplateColumns}}`**——内联样式没有断点，浏览器一放大就崩成两条竖线（饮食页原来就是这个毛病）。`minmax(0,…)` 也不能省，裸 `fr` 会被长内容撑破、整页横向滚动。
- **路由**：`App.tsx` 用 **hash 路由**（`#/<module-id>`，`parseHash()`），刷新停在当前页、不回仪表盘；GitHub Pages 无需服务端配置、Tauri 单页也通用。`setView` 改 `location.hash`，监听 `hashchange` 支持前进/后退。**parseHash 只取第一段做模块 id，子路径归模块自己解析**。

  ⚠️⚠️ **铁律：模块内会「换页」的状态一律进 hash 子路径**（2026-07-28 Rosie 拍板，原话：「以后这种小板块都默认加自己的 url，不然我每次在表格里刷新还要调到小表格界面重新进入」「原先语文英语也是的老是一刷新就回外面了」）。凡是列表→详情、板块切换、今日/一周这类 tab，**绝不能只存在组件 state 里**——否则刷新、误触后退、从别处跳回来都被弹回模块首页。
  - 公共实现 `src/lib/hashRoute.ts`：`useSubPath(moduleId)` → `[段数组, nav(段数组)]`，`nav([])` ＝回模块首页。**别再各模块自写 hashchange 监听**（原 study-log 的 `boardFromHash` 已并入它）。段值会 encode/decode，畸形 `%` 转义不会崩页。
  - 已接入：`#/mini-table/<表id>`（哪张表）、`#/study-log/<board>` + `#/study-log/book/<书id>`（板块 + 翻开的那本书）、`#/study-plan/<week|roadmap>`（今日＝无子段，默认值不占段、URL 更干净）。
  - ⚠️ **带 id 进来但数据还没读完**：先渲染「加载中…」，别闪一下列表再跳详情（mini-table 用 `loaded` 标志）。删掉当前打开的那条时要 `nav([])`，否则停在死 id 上。
  - ⚠️ 多级路径下「返回顶层」只调最外层那个 setter 一次即可（study-log 踩过：`setBoard(null)` 后又补 `setOpenBookId(null)`，后者 `nav(["book"])` 反而跳进书籍板块）。

## 模块现状

注册在 `registry.ts` 的有 5 个（**id 不变、显示名 2026-07-21 改了**）：`study-plan`（显示名**时间轴**，首位）、`todo`（待办）、`supplement`（饮食）、`study-log`（显示名**日日学**）、`mini-table`（小表格）。仪表盘壳子显示名=**今日总览**。（下文模块段落里出现的旧名"学练计划/学习记录"即时间轴/日日学。）

（原独立的「此刻 `focus`」模块已于 2026-07-20 并入学练计划的「今天」页并删除；时间轴逻辑现直接写在 `study-plan/index.tsx`。计划表 planner 更早于 2026-07-19 移除，其 `plan_tasks` 表由迁移 v4 / 浏览器 `DROP TABLE` 清掉。）

- **待办 `todo`**：单一列表 + 五个筛选框（四象限 iu/in/nu/nn + 今天，可叠加筛选）。筛选框含「全部」（清空筛选看全部）。**筛选框与添加框联动**（2026-07-20）：点某象限框→添加表单默认象限=该象限；点「今天」框→添加默认勾今天。**已撤掉「今天的学练计划」镜像**（2026-07-21）：待办页只管工作，不再镜像养生/英语等计划。**待办↔工作双向**：待办里标「今天」的（任意象限）→ 学练计划「工作」域显示；学练计划「工作」域顶部有输入框，加一条 → 建一条**今天·重要紧急**待办（`createTodo(title,"iu",today,…)`）。**已完成待办按完成日期分流**（2026-07-21，按 `done_at`）：**今天完成**的留在待办主列表**最下面**（`FinishedRow`，green 已完成 + 划线 + 「做了：笔记」）；**今天以前**完成的收进折叠「历史已完成」（同 `FinishedRow`，带完成日期，笔记回看用 `listLatestNotes` 跨日期取最新）。学练计划「工作」域同理：只显示未完成 + **今天完成**的，更早的不在此显示（去待办历史已完成看）。今日视图下方镜像显示**今天的学练计划**（活读取 study-plan 今日条目，虚线框、可勾选写回 plan_checks，不复制记录；**可折叠、默认收起**（用 `<Collapse>`），标题带条数，免得长列表挤掉待办；镜像里**不显示明细时间**，时间只在学练计划页留；待办条目框已放大）——即学练计划自动同步进待办今天；反向"手动加的今天待办→此刻工作"已由 focus 工作域实现（同一份 todo 数据）。每条待办有「今天」开关（点击设/清 `due_date=今天`）和**象限彩签（`QuadrantTag`，点一下展开四个象限直接挑）**，标题可点击就地改。⚠️ 象限彩签 2026-07-29 先做成「点一下跳下一个」的循环，当天被 Rosie 否了（「能不能改成点一下选变成哪个，而不是必须得按顺序点一遍才行」）——循环最坏要点三下，每点一下都写库、还要经过两个不想要的中间状态。现在是小浮层菜单，关闭靠一层 `fixed inset-0` 透明底罩（不用 ref + document 监听）；⚠️ 它的祖先不能有 `overflow-hidden`，否则菜单被裁。右侧窄栏嵌入打卡。已完成的待办收进**「历史已完成」折叠区**（`<Collapse>` 默认收起、放最下面、每条标完成日期 `done_at`，带「清除已完成」），免得堆着占地方。含**每周复盘**（`WeeklyReview.tsx`）：周一~周五完成情况，周五 16:00 起到周末页顶出现横幅，平时点「本周复盘」可看。
- **打卡 `habit-checkin`**：`HabitPanel` 组件（有 `compact` 窄栏模式 + `weekly` 周表格模式，被待办页右栏复用；**待办页用 `compact weekly`**，右栏加宽到 380px），**未单独注册**到 registry。**周表格（2026-07-20，待办在左/打卡在右版式）**：行=全部习惯、列=本周一~日（`mondayOf`+7 天，表头今天列高亮），格子按 `habitOnDay` 决定是否可打（不排的天显示「·」）、`habit_checkins` 按(habit,date)读写可补卡、未来未打卡不可点、今天列描边高亮；打卡本身仍是方框式点按（快速多点），非计划的三态。习惯名可就地改。habits 表带 `days`('*' 或 '1,2,3,4,5')+`sort_order`（迁移 v10），`seedHabitsIfEmpty` 首次写入预置清单：工作日=喝水×2/工作站立×4/眼保健操×2/肩颈拉伸/午休，周日=打扫卫生（搓澡已由 Rosie 删除、2026-07-20 从种子移除；打扫卫生同日改为仅周日）；面板按今天星期 `habitOnDay` 过滤只显示当天该打的。**添加习惯时可选重复星期**（一~日七个小方块，全选=每天=`'*'`，否则存 `'1,5,6,7'` 这类；另有**每天/工作日/周末快捷预设**`DAY_PRESETS`，添加表单和「改重复日」编辑器都有）；`daysLabel()` 把 days 转人话，每行末尾显示 days 徽标。若新建的习惯今天不该打卡，表单下会提示「已添加…每周X，今天不显示」免得以为没保存。（"几周一循环"隔周频率暂未做，当前都是每周重复；真要隔周再加 week_interval 列。）
- **学练计划 `study-plan`**：六线周计划（养生/运动/英语/HCIP/AI方向/阅读），按 Rosie 作息表带时间段。**今日视图=时间轴布局（原「此刻」并入，2026-07-20）**：左侧连线时间轴列六个领域（养生6:10/英语7:30/工作9:20/学习19:00/运动19:40/阅读21:00/**睡前21:40**，`DOMAINS` 各带 start 分钟数+颜色，节点间距 gap-14。**养生按时间拆分**（2026-07-20）：`Domain` 带 `timeMin/timeMax`，养生节点只收上午条目（timeMax 720），新增**睡前节点**收 18:00 后的 wellness（泡脚21:00/睡前拉伸21:40，timeMin 1080），`slotStartMin()` 解析 time_slot 起始分钟），按当前时间自动定位（`autoDomainKey`：最后一个 start≤now）、红虚线标"现在"，点领域手动切、"← 回到此刻"复位。**周末隐藏「工作」域**（2026-08-15，Rosie 周末不上班）：`Domain.weekdaysOnly`（工作域设 true）；`Page` 里 `isWeekend=todayNum∈{6,7}` 时 `domains=DOMAINS.filter(!(weekdaysOnly&&isWeekend))`，`autoDomainKey`/`active`/当前时间轴/今天分组卡三处都用过滤后的 `domains`。右侧是该领域的**大卡片**（`ThreeRowCard`）：①该项**明细时间**(time_slot 徽标)+标题+打开按钮+**【已完成】【未完成】两按钮**(DoneToggle) ②详细解释(detail) ③网址 ④"我做了什么"笔记。**打勾改成三态两按钮**（2026-07-20）：今日卡片、一周列表(ItemRow)、待办页今日计划镜像三处的方框勾都换成 DoneToggle；**三态＝待做/已完成/今天做不了(skip)**，存在 `plan_checks.status`('done'/'skip'，NULL 视作 done；迁移 **v11**，四处同步：lib.rs+schema.ts SCHEMA_SQL+BROWSER_MIGRATIONS+supabase/schema.sql，Rosie 需到 Supabase 跑 `alter table public.plan_checks add column if not exists status text;`)。数据层：`listCheckStatus`(item→status map)、`setCheckStatus(id,date,status|null)`；`listChecks` 仍只返回 done 集（skip 不计入完成/宽限）。UI：**待做排上面、已决定(done/skip)沉到下方**（`pendingFirst`/镜像 sort），让上方只剩要做的；今日卡片额外显示每项明细时间（原来只有左侧时间轴的领域总段时间）。工作(todo)源无 skip，未完成键对其为空操作；**工作域已完成的 todo 不再消失、沉到该域最下**（2026-07-20，`todoCards` 改为含 done 并按完成排序，与待办页一致）。笔记门控同 focus：英语/学习/阅读**必填才能勾**（`noteRequired`），养生/运动/**工作**选填（工作 2026-07-20 改非必填）。数据源：养生/英语/学习/运动/阅读=对应 track 的 plan 条目（走 plan_checks/plan_notes）；**工作=todo 里 due_date≤今天的项**（toggleTodo 打勾、复用 plan_notes 存笔记）。经期开关（periodOn）与今日视图共用同一 state，切换即时生效。**计划外新增（2026-07-21）**：每个计划领域今日视图底部有「加一条计划外的」输入，`addExtra(track)` 建一条 `days=今天星期` 的 plan_item（当天记进该 track，会按那个星期重复）；**删除规则（2026-07-21 定稿）**：`isSeedItem(item)` **按 id 判定，不按名字**——`SEED_IDS` = 用 `seedUuid('plan_item:{track}|{title}|{time_slot}')` 对每条 `SEED_ITEMS` 算出的确定性 id 集（与 `seedIfEmpty` 生成方式完全一致），`SEED_IDS.has(item.id)` 即原定计划、不给删；自己加的「计划外」是随机 id，才显示删除（`onDelete`）。**内容指纹兜底（2026-07-28 加）**：光靠 id 有个盲区——id 是**播种那天**按 `track|title|time_slot` 算死的，后来把某条种子的 track/标题/时段一改，key 就变了，**早先播下的那行 id 仍是老值、不在 `SEED_IDS` 里，于是被当成「计划外」给出删除按钮**（正是 Rosie 会误删原定计划的那类坑）。所以 `isSeedItem` 改成 `SEED_IDS.has(id) || SEED_KEYS.has(track|title|time_slot)`，`SEED_KEYS` = 当前 SEED_ITEMS 的内容指纹集。**顺序不能反**：经期 swap 会把 title 换成 period_title，那时只有 id 认得出来；而「计划外」由 `addExtra` 建、没有 time_slot，指纹永远配不上带时段的种子，不会被误锁成不可删。踩到的实例：**足弓重建**（2026-07-20 播种时 key 与现在不同 → 云端那行 id `2ed40c4e…`，当前种子算出来是 `dffd41cd…`，对不上）。⚠️ **踩坑**：早先按名字比对，经期开关开时 `applyPeriod` 把 title 换成 period_title、加上就地改名，都会让原定条目被误判成「计划外」而给删（Rosie 误删过腰椎稳定）——id 从建库起固定，最稳。今日大卡片(`ThreeRowCard`)与一周列表(`ItemRow`)两处都用它门控删除按钮。**计划外条目可加链接**：`ThreeRowCard` 收到 `onSetUrl` 时在详解下渲染一行可就地编辑的「链接」（`EditableText`，空显示「＋加个链接」），写回 `updateItemUrl`；原定条目不显示该行（链接由种子/添加表单给）。**「今天没勾＝没完成」**是默认语义（打卡按日期存 plan_checks，过了今天不再补）；**唯一例外＝睡前拉伸**：昨天该做却没勾的，今早今日页顶会出现青色「补勾昨天」横幅（`graceItems` 按标题含"睡前拉伸"+昨天该做+昨天未打勾筛出，点按钮 `toggleCheck(id, 昨天)`），因为它常在临睡/过零点才做。要给别的条目也开次日宽限，就扩展 graceItems 的标题判断。八段锦武当版（一三五日）与国局版+足弓（二四六）隔天交替。晚间运动周主题用 Rosie 收藏夹里的欧阳春晓/C戈跟练（一=瘦斜方肌，二=芭杆手臂薄背，三=太极，四=出门散步1小时[周中出门，不出门则室内无跑跳有氧]，五=骨盆稳定，六=出门散步1小时[周末第2次出门]，日=泡沫轴恢复[周日下午搓澡+休闲不出门]）。每周≥2次出门（周四+周六）走满1小时。⚠️⚠️ **散步日只排散步**（2026-07-30 Rosie：「只要是散步，那么运动页就只允许放散步，不要别的」）：`腰椎稳定 10 分钟（每晚开场）` 的 `days` 从 `'*'` 改成 **`'1,2,3,5,7'`**——它的角色是视频跟练前的开场，而四/六没有视频跟练、出门 1 小时本身就是全部内容，再垫 10 分钟只会让人出不了门。**做法＝改计划数据，不是加显示层过滤**：`domainItems` 那种过滤只作用于「当前／今天」，`一周` 视图走的是另一条路径、会照样列出来，两边对不上就成了新 bug；而且计划里根本不该有这条，藏起来不如不排。⚠️ **刻意没有 bump `SEED_VERSION`**：`days` 不在 `seedUuid` 的 key（`track|title|time_slot`）里，所以 id 没变、`SEED_IDS`／`SEED_KEYS` 照样认得它、`plan_checks` 打卡历史不断；而 bump 会请她点「一键同步」＝`resetToSeed`＝**清掉自定义条目和勾**，为一个 `days` 值付这个代价不值。已直接 PATCH 云端那行（`07f55165…`）让所有设备同步拿到，`seed.ts` 同步改好给将来的新库。**以后只改 `days`／`detail`／`url` 这类不进 id 的字段都照这个走：改 seed + PATCH 云端，别 bump。** 表 `plan_items`（days: `'*'`=每天或 `'1,3,5'`，1=周一）+ `plan_checks`（每日打卡，UNIQUE(item_id,date)），迁移 v5。**补卡（2026-07-21）**：一周视图**「今天及以前」的每天都可勾**（漏打卡倒回来补；将来的天不可勾）——`weekChecks`(本周各天 listCheckStatus) + `setStatusForDate(item,date,next)` 写对应日期；`ItemRow` 加 `noteGate`（补卡过去的天不门控笔记，只有今天要求先写笔记）。**四个视图 tab（2026-07-28 改版）：当前 / 今天 / 一周 / 路线**（`PLAN_TABS`、`TAB_LABEL`，**当前＝无子段是默认页**，`#/study-plan/today|week|roadmap`）。⚠️ **当前与今天的分工别搞混**——「当前」＝此刻该干什么（竖线时间轴自动跟随时间 + 当前领域按状态分栏，进来就动手），「今天」＝全天一览（紧凑清单，一条一行、一屏扫完、随手补勾，**刻意不放笔记框和详解全文**）。别让「今天」也去做整天时间轴，那就跟「当前」职责重了（原方案 B 的「单栏整天时间脊」就是因此没选）。
  - **当前**（设计＝方案 A 左栏 + 方案 C 右栏）：左侧竖线时间轴每站挂 `MiniBar` 迷你进度条 + `done/total` + 做完打 ✓，所以哪条线做完了不用点进去就看得见；页顶有「现在 HH:MM · 自动跟随」红色药丸 + 今日总进度条。右侧**按三态分栏**（`ColHead`）：左栏「还要做」放能写笔记的大卡片（`ThreeRowCard`），右栏「已完成」/「今天做不了」用窄条 `DoneStrip`（保留笔记、可一键撤销回待做）。⚠️ **三态必须分清**（Rosie 要求）：skip＝主动决定今天不做，不能跟「还没做」混在一栏。工作域来自待办、没有 skip，所以不渲染那一栏。
  - **今天**：按领域分组的 `CompactRow`（时间｜标题｜详解截断｜三态键｜视频），分组头带该线进度条和「去处理 →」（跳回「当前」并选中该领域）。⚠️ 英语/学习/阅读的笔记门控仍生效：这里没有笔记框，所以那几条的【已完成】是禁用的，`disabledHint` 提示去「当前」写。
  - ⚠️ 领域的条目过滤（养生只收上午 / 睡前只收 18:00 后）抽成了模块级 `domainItems(domain, list)`——当前的大卡片、左侧进度条、今天的清单**三处共用**，别再各自复制一份 filter。每个领域的完成度走 `domainProgress(d)`（工作域走 todos）。
- 原**今日/一周/路线**三个视图：今日可勾选、条目带跟练视频链接（Tauri 用 plugin-opener 开系统浏览器，网页开新标签）；路线页渲染 `seed.ts` 里的 `SEMESTER_PLAN`（2026 下半年 6 个月计划：每月体重目标+四线目标+每周焦点，验收日 12/27，总目标见 `SEMESTER_TARGET`——体重 ≤58 达标/56.5 优秀，55 顺延 2027Q1，50 不设）。4 周递进周期（适应/完整/加量/减载），起点存 `app_settings.plan_cycle_start`。首次打开自动播种 `seed.ts`（seedIfEmpty 有 StrictMode 并发守卫）。**改 SEED_ITEMS 后必须把 `SEED_VERSION` +1**：已播种设备会在页顶看到黄色「计划模板有更新」横幅，点「一键同步」触发 `resetToSeed`（清自定义条目和勾，版本号存 `app_settings.plan_seed_version`）。**学习线优先级（2026-07-20 重排）：AI 主线、华为认证副线**——周三晚 19:00+周六下午 14:00-16:00=华为认证(冲 HCIE，从 HCIA→HCIP 推进，周六下午做 eNSP 实验)；周一二四五晚=AI 刷课；周六日上午 09:20-12:00=AI 项目(NOC Sentinel 补 evals+数据、life-dashboard 加 AI 功能，完善后写进简历)。AI 课路线从"AI 是什么"开始（吴恩达 AI for Everyone 中字→3B1B→李宏毅生成式AI导论→Prompt→RAG/Agent→Evals）。英语：摸底 A2（小学摸底 19/20，基础词/拼写/简单句扎实，真正缺的是语法系统和中级词汇；敢开口是亮点）。主线教材=新概念英语第一册（主教材=刘羽Leo 199集含系统语法 BV1xa411J7jJ、纯动画备用 BV1MT4y1o7Cv、影子跟读 BV1PJ4m1M7a3），前约24课可加速、第25课起放慢；单词用「不背单词」PEP词库（已背完一年级+二上，二下起，~10词/天起步、时间盒20min、复习优先）；新东方四级词汇暂缓到 11 月；《查莉》降为中字磨耳朵素材；12/27 里程碑=无字幕听懂 50-60%。**英语用「固定流程+进度指针」而非日历式排课**：每天流程一样，学完一课才进下一课，进度由 Rosie 自己推进（她是复健期学习者，强调不赶日期、重连续性）。晨间英语块（07:30-09:10）：①复习10′ ②学新课55′（Leo课，新概念奇数课=新课文≈35′视频、偶数课=练习留次日复习，一天不必啃完整单元）③朗读+跟读今天课文25′（跟课文自身音频，弃用之前那个过短的影子跟读视频）。④不背单词 PEP 移出晨间→通勤/到公司后背（时间自由）。段间起身5分钟（护腰）。**英语条目带每日进度笔记**：`plan_notes` 表（迁移 v9，UNIQUE(item_id,date)），英语/学习(cert+ai)/阅读条目在今日视图下方有输入框写进度（英语"刷完001"、学习"看了哪个视频"、阅读"看到哪本书哪里"），onChange 即时存库；这几个 track **没写笔记不给打勾**（canCheck 门控，needsNote=english/cert/ai/reading）。养生/运动不门控。运动规则：生理期不安排腹部相关（卷腹/侧桥/平板类训练，以及仙人揉腹这类腹部按压）。已做成**经期开关**：**开关按钮 2026-07-20 移到仪表盘（`DashboardShell` 头部，全局唯一入口，其它页不再放按钮）**，study-plan 页头只显示只读状态徽标「🩸经期中·已避开腹部」；状态存 `app_settings.plan_period_on`；plan_items 带 `period_action`('skip'隐藏/'swap'替换)+`period_title`+`period_detail`，UI 用 `applyPeriod()` 变换（迁移 v6）。种子里揉腹=skip，腰稳/芭杆/心肺=swap 成经期安全版。新增经期敏感条目时给它设 period_action。阅读书单原则：历史类只收史实可靠的（学术著作或明确标注虚构的史实扩写），回避被史学界指出个人偏颇的通俗史（如《明朝那些事儿》——Rosie 要求等自己历史体系完善后再读）。
- **饮食 `supplement`**（模块 id 仍是 supplement，显示名「饮食」）：补剂时间表硬编码在 `SCHEDULE`（1=周一..7=周日，改补剂就改它）。Card 显示今日早/午/晚，Page 有三段：**今日补剂**（只显示当天早/午/晚，2026-07-20 从整周表改成仅今天）+ **三餐推荐**（静态 `MEALS`，每餐给一个自己做 + 一个减脂外卖）+ **奶茶打卡**（DB 表 `treat_log`，迁移 v7，一杯一条，`data.ts` 里 logTreat/undoTreatToday/treatStats，显示今天/本月杯数）。**接了经期开关**：`getPeriodOn()` 为真时补剂区直接显示「🩸 经期：无需进食补剂」（2026-07-20；原 `PERIOD_SCHEDULE` 已删，经期停所有保健品）。即🩸开关同时管运动避腹 + 经期停全部补剂。**经期饮食建议**（2026-07-22）：periodOn 时三餐区顶部显示粉色「🩸经期饮食建议」框（`PERIOD_MEAL_TIPS` 常量：涨重是存水非脂肪/守基代别少吃/少盐/多水/补钾补铁等），平时不显示。**饮品打卡**：`treat_log` 扩展了 subtype(奶茶/果茶/酸奶)/brand/name/sugar/calories（迁移 v8），页面右上角月历按品类给有饮品的日子上色（奶茶红/果茶绿/酸奶紫），「记一杯」表单填品类+品牌+名字+糖度+可选热量。**补记（2026-07-28）**：`DrinkCalendar` 点某天＝选中它（`selected/onSelect`，今天及以前可点、未来禁用），「记一杯」写到选中日期（`drinkDate` state，默认今天，表单顶显示「记到 X 补记 / 回到今天」），下方"已记"列表也跟着显示选中那天的——解决了以前只能记今天、漏记补不回来的问题。**补剂可点打卡（2026-07-29 加，Rosie：「补剂给我改成可以点击是否已经吃了的，点了就是已服用，改成别的颜色」）**：每颗药是个按钮，点了变**绿底白字 + ✓**，再点取消；标题旁显示「已服用 N/M」、全吃完变绿字。⚠️ **复用 `app_settings`、零改库**（同体重库的做法，她不用去 Supabase 跑 SQL）：key ＝ `supp:<日期>:<slot>:<补剂名>`，值 `"1"`＝已服用、`""`＝没吃；`getSuppTaken(date)→Set<"slot|name">`、`setSuppTaken(date,slot,name,taken)`（组件里 import 时改名 `setSuppTakenDb`，别跟 state setter 撞名）。⚠️ **一颗药一个 key，别把一天塞成一条 JSON**：同步是"最后写入胜出"，整天一条的话两台设备当天各点几个、后同步的那台会把另一台的勾**整片盖掉**（体重当初就是为这个拆的 am/pm）。⚠️ **取消打卡写 `""` 不是 DELETE**：删了会被别的设备的旧行"复活"（同步没有墓碑）。时间戳同样走单调递增。**三餐记录**：`meal_log`（date+meal 唯一，v8），每餐可写"我吃了什么"+热量，`dayCalories()` 汇总当日三餐+饮品热量。⚠️⚠️ **三餐改成显式「确认提交」，不再 onBlur 自动存**（2026-07-29 Rosie：「给三餐框里都加个确认提交按钮，提交后内容有修改需要再次点击确认提交，现在实时同步有概率搞错掉」）：抽出**模块顶层**组件 `MealRow`（草稿 state 在它自己身上），内容框是 textarea（**回车提交 / Shift+回车换行**）+ kcal 框 + 按钮。「有修改要再点一次」不是另存的标记位，而是 `dirty ＝ 草稿 ≠ saved` 推出来的——提交后父级 `submitMeal` 把 saved 更新成新值，dirty 自动变 false、按钮回到「已保存 ✓」。⚠️ **提交时要把草稿也 `trim()`**，否则父级存的是 trim 过的、草稿还带末尾空格，dirty 永远为真、按钮卡在「确认提交」。⚠️ 草稿**刻意不写 effect 跟 props 同步**：那样云同步一拉到新数据就会冲掉她正在打的字；saved 变了而 dirty 仍为真＝她有未提交的改动，保留草稿才对。⚠️⚠️ **正因为不同步，调用方必须等库读完才渲染它**——`mealsLoaded` 门控 + `key={`${today}-${m.key}`}`。上线当天就踩了：父级 `meals` 初值是空串、`getMeals` 是异步的，组件先挂载 ⇒ 草稿定格成空 ⇒ 库里明明有「一个鸡蛋 50」，界面却是空框 + 亮着的「确认提交」，**一点就把记录清空**（Rosie 截图里看出来的）。加 effect 同步也能治，但会把"打字被冲掉"带回来，所以选门控；`getMeals` 失败也要 `finally` 放行，否则永远卡在「读取中」、连手填都做不了。⚠️ 凡是「草稿只取一次初值」的组件都有这个坑，别只记住这一处。（体重、饮品、零食仍是即时保存——它们是单个数字/一次性提交，没有"打一半"的状态。）⚠️ 热量由 Claude 在对话里算（App 不内嵌模型、不自动估算），用户把数字填进热量栏。**零食打卡（2026-07-28 加）**：⚠️ **复用 `treat_log`、不建新表**（Rosie 拍板「省事，零改库」，她不用去 Supabase 跑 SQL）——用 `kind` 区分 `'drink'`/`'snack'`，`subtype` 存品类（`SNACK_SUBTYPES`＝坚果/酸奶/黑巧/水果/其他）。附带好处：`dayCalories()` 按 `subtype IS NOT NULL` 汇总 treat_log，**零食热量自动进当日总摄入、不用改算法**。⚠️ 代价与坑：①表名叫 treat_log 却装零食，名不正言不顺（真嫌乱以后再拆表）；②`listDrinks` **必须**排掉 `kind='snack'`（`kind IS NULL OR kind <> 'snack'`，NULL 是 v7 只有奶茶时代的老行，仍算饮品），否则零食会混进饮品列表和月历配色。`logSnack/listSnacks/deleteTreat`（删除饮品零食共用）。饮品和零食**共用同一个 `drinkDate`**，所以点一次月历两边都补记到那天；月历里只有零食没饮品的日子用**琥珀内描边**（跟饮品的填色区分，一格能同时表达两件事）。
- ✅ **修：饮品/零食热量没进饮食页预算**（2026-07-28）。`dayCalories()`（今日总览、小表格用）一直是三餐+treat_log，但饮食页自己算 `eaten = 早+午+晚`、`dinnerAllow = 目标−早−午`，**记了奶茶两处数字对不上**。现在 `treatCal`＝当天饮品+零食热量，一起进 `eaten` 和 `dinnerAllow`，预算条上多一段**琥珀色**、图例显示「（含饮品零食 N）」。
- **页面布局（2026-07-28 重排）**：顶部**今日卡路里**面板 + 下方两列网格——**三餐左侧纵跨全部四段**，右栏四段＝**补剂 / 饮品打卡 / 零食打卡 / 月历**（`gridTemplateRows: "auto auto 1fr auto"`，**第三行 1fr 让零食那段吃掉剩余高度**，治 Rosie 说的右下一大片空白「加长」；月历放最后一行 auto，别让它拉伸变形）。共用小组件 `DateLine`（记到 X／回到今天）、`TreatRow`（已记的一条），两个表单别各写一份。（原 2026-07-20 版是三餐纵跨两行 / 补剂右上 / 奶茶右下。）图标 `Pill`→`Utensils`。**卡路里预算**：`BMR` 常量=1420（Mifflin，女 164/68/24 往低估）；每日目标存 `app_settings.cal_target`（默认 1400，`getCalTarget/setCalTarget`，页头数字框可改、跨设备同步）；**晚餐可吃 = 目标 − 早 − 午**（读 meals 的 calories 实时算）；预算条含各餐分段 + **目标标记 + 基代红线**（版本1条+版本4红线合并），目标低于 BMR 时红字警告"当心掉发"；减脂安全区 1420–1550。Rosie：24岁，别信减脂App默认的 1200（低于她基代=掉发区）。
- **学习记录 `study-log`**（**2026-07-20 改版为六大板块 v2**，原「科目+时长」已弃）：**主界面＝六个彩色方框**（田字：英语·语文 / AI·历史 / 书籍·电影），每框左图标+名+条数、**右侧露出内容预览**（学习板块显示最新一条摘要；书籍/电影露封面缩略）；点方框进对应板块、左上"← 学习记录"返回（模块内自管 `board` state，非路由）。数据＝**一张通用表 `study_entries`**（迁移 **v12**：board/kind/entry_date/title/body/meta(JSON)/status + 同步字段；`data.ts` 里 `listAllEntries`(一次取全在组件里分组)/`createEntry/updateEntry/deleteEntry`）。
  - **每条内容「今日看完」判定**（2026-07-21，统一函数 `entryDone(e)`）：**要默写的内容（古诗/精读文章/英语谚语 `needsDictation`）＝该默的都默过≥1遍才算看完**（判定唯一实现＝`dictState(meta)`，头部只读徽标「默写后算看完/✓已默写」，不能手动标；⚠️⚠️ **文章和单词本各记各的**（2026-07-30 Rosie：「我默写完单词给文章也标成已默写了，改一下，把单词本和文章独立开，仅仅前端显示是叠加在一起的」）——原来是 `artAtt || wordAtt`，默完任一边整条就算看完，于是默了单词、文章一字未写照样顶着绿徽标（`eng-20260720-1` 就是这状态，已随本次改动退回未完成，是对的）。现在 `allOk = artOk && (没有单词本 || wordOk)`。⚠️ **不能简单换成 `&&`**：古诗和英语谚语走 `EntryDoc`、只写 `artAtt`、**没有单词本**，一律要求 wordAtt 会让它们永远无法完成（没配生词的精读文章同理）——所以「要不要看单词那边」由**有没有单词本**决定，不由 kind 决定。前端＝一张卡叠着显示，但**三枚徽标各自独立**：顶部总状态（两边齐才变绿，未齐时显示 `文章 ✓ · 单词 —`）＋「默写文章」旁的「✓ 文章已默写」＋单词本标题旁的「✓ 已默写」（`WordBook` 的 `dictated` prop）。**别在组件里重算这条规则**，读 `dictState`；英语谚语 2026-07-27 加入——`EntryDoc` 里 `canDictate=古诗||board==='english'&&kind==='谚语'`，默写目标 `dictTarget`＝meta.recite‖古诗抽【原诗】‖英语谚语取 body 第一行英文句，复用 `ArticleDictation`，按钮「默写这句」）；**带练习题的＝交了作业即完成**（2026-07-27 起，统一判据 `gradedByHomework(e)`：看 `meta.homework` 有没有内容——Claude 批改后写进 homework 就自动算完成，头部只读徽标「交作业后算完成/✓已完成」，不手动标）。**覆盖范围＝`meta.exercise === true` 的条目 + 语文练笔**（练笔的 body 就是那道题，硬编码兜底）。⚠️⚠️ **2026-07-30 去掉了 PM 的 board/kind 硬编码**（原来 `board==='pm' && kind in (PM概念|产品拆解|练习)` 整块归批改判定，是 2026-07-29 为「从标记看完改成批改练习后算看完」加的）。**为什么必须去掉**：新节奏是「每天最多一道作业」（见喂养段的轮换表），但 PM 内容她要天天看——硬编码把**内容**和**作业**焊死了，任何 PM 条目不管出没出题都顶着「交作业后算完成」、没法手动标已读，于是轮换根本落不了地。现在统一到显式标记：出题写 `exercise: true`，没出题的 PM 条目回到普通「标看完」。这也正是 `meta.exercise` 立起来时定的原则——**判据要显式，别拿 board/kind 猜**。⚠️ **迁移已做完**：云端 10 条老 PM 条目全部补了 `exercise: true`（其中 `pm-20260728-teardown`／`pm-20260729-concept`／`pm-20260729-teardown` **没有 `meta.done`**，漏补就会掉回未读）——以后再动这个判据，先跑一遍「有 homework 但没 exercise 也没 done」的审计。原来那三条打包的「练习批改 ①②③」条目已拆散挂回各自题目并软删，**批改跟题目一一对应、不再单开练习条目**）。⚠️ 所以给 PM 批改时是 **PATCH 那道题自己的 `meta.homework`**（如 `pm-20260728-concept`），不是新建 `pm-<日期>-exercise`；**其余内容＝手动标 `meta.done`**（头部「标看完/✓已看完」开关）。看完的条目变淡。没标/没默＝没来得及、内容不换明天接着看。Landing 每学习板块显示「今日 X/Y 看完」（今天条目里 entryDone 的比例）。
  - **去重（2026-07-21）**：`LearningBoard` 与 `EnglishBoard` 的"按日期分组折叠"抽成共用 `DateGroupedBoard`（传 `renderItem` 决定每条渲染成 EntryDoc 还是 ReadingCard）；打卡加习惯/改重复日的七个星期方块抽成 `WeekdayPicker`；纯"输入+按钮"统一用 `QuickAdd`。
  - **英语/语文/AI/历史**（`LearningBoard`）：**当天条目展开、历史按日期折叠**（点日期头展开，今天默认开），所有看过的都留记录可翻。内容**由 Claude 更新**（见下"内容注入"）；也有个「加一条」次要入口供手动/测试。展示为主（`EntryDoc` 只读+删）。
  - **语文古诗默写 + 复习板块**（2026-07-20）：`EntryDoc` 对 `kind==='古诗'`（且传了 onPatch）显示「默写这首」——复用 `ArticleDictation`（默写时原文 `Blurred` 高糊，批改留 3 遍存 meta.artAtt）。⚠️ **只默写「诗本身」**：默写目标 = `meta.recite` ‖ 从 body 抽【原诗】段（`/【原诗】([\s\S]*?)(?=\n*【|$)/`）‖ 整段兜底——因为古诗 body 常含【原诗】【注释】【白话】等大段，不能拿整段当答案（否则命中率乱、漏错全是注释字）。**注入古诗内容时最好把纯诗放进 `meta.recite`**。⚠️ `updateEntry` 时间戳**单调递增**（内容由 Claude 用固定/未来时间戳注入，默写记录等用户编辑必须严格更新才不会被同步盖回丢失——同 mini-table 那个坑）。`ArticleDictation` 批改**中文按字、英文按词**（`tok()`：有汉字按汉字命中率，否则英文单词）。**`review` 复习**（`ReviewBoard`）：聚合到期的语文古诗 + 英语精读。**间隔复习（2026-07-28 加，原来只拉昨天、过一天就再也不出现＝学过的在漏）**：排程逻辑抽到 `study-log/review.ts`（纯函数，别在组件里重写），五次全过＝毕业不再进队列。进度存 `meta.revs`（历次通过日期数组，升序），**读时兼容旧的单值 `meta.reviewedAt`**（当作 `[reviewedAt]`），写时两个都写。到期判定 `isDueToday`＝`nextDue<=今天` 且 `revs` 不含今天——「今天已过」这条排除很关键：长期没打开时一条内容**一天最多推进一级**，不会把五轮全怼到脸上，而是每天补一轮慢慢追上。
    - ⚠️⚠️ **排程锚点＝「上一次实际复习那天」，不是学完那天**（2026-07-29 Rosie 拍板推翻原设计，原话：「艾宾浩斯遗忘曲线这个要灵活一点呀，应该是按我第一次复习默写算第一天，而不是固定日期，不然我逾期复习就一直放在这吗」）。常量改成 **`REVIEW_GAPS=[1,2,4,8,15]`**＝第 n 次距上一次的天数（是原 `[1,3,7,15,30]` 的**差值**，所以对按时复习的人排出的日期跟以前一模一样，只有拖延时才不同）。`anchorOf(e)`＝`revs` 最后一天 ‖ `entry_date`，`nextDue = anchorOf + GAPS[stage]`。
      **原来锚死 `entry_date` 的错在哪**（原文档还专门写过"锚点是 entry_date 不是上次复习日"，是错的）：7/21 学的《塞下曲》她 7/29 才打开，第 2 次早在 7/24 就"到期"了——她刚默完第 1 次，这条**立刻又逾期 5 天**卡在列表里，永远还不完；而且间隔的意义也没了，同一天连做两轮等于白做。现在拖了就顺延、不倒扣，逾期徽标改叫「晚了 N 天」并注明默过就顺延。`reviewSteps` 里未来几格的计划日期也必须**顺着锚点往后推**（一个 cursor 累加），不能各自从 entry_date 算，否则拖几天后面几格全成"已逾期"。
    - ⚠️ **默到全对的条目立刻从列表消失**（2026-07-29 Rosie：「我默写全对那就应该算完成复习了，就从复习里消失了，现在是全对也还在里面，这不对呀」）。原来反着做过：怕卡片被抽走很跳，用 `justPassed` 把通过的留在页面上显示「✓ 通过 · 下次 X月X日」——**方向错了**，列表是待办、做完就该清掉，留着反而看不出还剩几条。现在 `items` 只 filter `isDueToday`，完成反馈改成顶部一行 `passedNow`「✓ 刚默完 N 条：X（下次 8/2）」——条目走了但"刚过了啥、下次啥时候"还看得见。因此卡片内 `passedToday` 恒为 false，`passed` 一律传 false、不再有"通过态卡片"这个分支。
    - 页顶/空态有 `UpcomingLine` 显示接下来几天各几条。
  - ⚠️⚠️ **加复习类型前先算稳态负担**（2026-07-28 定的规矩）：间隔 5 段 → **每天到期条数 ＝ 5 × 每天新进队列的条数**。所以贵的要克制、便宜的可以多加。`REVIEW_KINDS` 是配置表（board/kind/label/**mode**），加一行就多一类。当前四类＋单词：古诗(默写,~4min) / 英语精读(默写,前2轮全文~9min·后3轮只默背诵句~2min) / 英语谚语(默写,~20s) / 成语(**recall**,~25s) / 单词(中→英,~5s)。稳态合计约 47 分钟——比改造前只有古诗+精读的 65 分钟还少。**刻意不进复习的**：历史（她自己定「当故事看、不背诵」）、练笔（输出型，复习就是再写一篇）、AI 新闻/趋势（有时效）、书影读后感、金融（偏理解实操）、PM 概念（复习形式应是拿新产品再套一遍框架，已由每日产品拆解承担）。
  - **两种复习形式（`mode`）**：`dictation` → `ReviewDictation`（整篇默→逐句订正→复验全对）；`recall` → `ReviewQuiz`（看提示答一个答案）。⚠️ **成语的方向刻意是反的**：出题给【意思】、答【成语】——她学成语是为了「治说话没文化」，真实场景是*想表达某个意思时能不能调出那个成语*，所以检索方向必须是 意思→成语；反过来只练认得出、练不出用得出。题面从 body 的【意思】节抽、答案取标题「成语 · X」里 `·` 之后那截。⚠️ `isReviewable` 对 recall 型额外要求「能抽出题面」，抽不出就不排——宁可漏也别排一道没题目的题。
    - ⚠️⚠️ **题面必须抹掉答案，泄题有两处**（2026-07-29 Rosie：「成语别写出处，又把原文露出来了」）：①【出处】几乎必然含成语本身（「读《易》，韦编三绝。」）——**整段不给**，出处不是复习必需的，`recallCard` 已不返回 hint、`ReviewQuiz` 删掉了那行；②**【意思】本身也含**！中文释义的惯用写法就是「……后用『韦编三绝』形容读书勤奋」，答案明晃晃写在题面里。所以 `recallCard` 把题面里出现答案的地方一律换成 `____`（**连外层引号一起吃掉**，否则会留下一对空引号把字数漏出去）。**这不是内容写错了**，释义带上词条是正常辞书体，注入侧不用改、读的时候遮掉就行——别去改注入的 body。另外卡片头部**不显示成语标题**（标题就是答案），只显示「（答对了才显示是哪个）」。
  - **精读降级**：`reviewTarget(e, stage)` + `isShortRound(e, stage)`——第 3 轮起只默 `meta.recite`（没有就退回全文首句），卡片上标「只默背诵句」免得以为文章丢了。**取原文必须传 stage**；刚通过的那条要回退一轮（`passedToday ? stage-1 : stage`）才是「她刚默的那份」。
  - **单词级 SRS（`wordReview.ts` + `WordReview.tsx`，2026-07-28）**：⚠️ **调度单位是单个词，不是「某篇精读的整个单词本」**——同一篇里 get up 可能一遍就记住、reluctant 反复错，按篇复习会让会的词陪着不会的词一起重复（这是 Anki 按卡片而不是按牌组调度的原因）。**不建新表**：进度存在所属精读条目的 `meta.wordSrs = { [英文原词]: RevRecord[] }`，到期算法跟条目共用**同一套锚点**——即 `上次答对那天 ‖ entry_date` 再加 `REVIEW_GAPS[stage]`（2026-07-29 跟着条目一起从"锚死 entry_date"改过来了，改一处别忘另一处）。代价是查队列要扫全部精读条目（`listAllEntries` 本来就全量加载，可接受）。题型 **中→英**（她短板是产出，看英文认中文只练识别），判对＝去非字母数字后小写全等（**拼写必须对**）。UI 里队列**在打开时定格**（`useState(() => dueWords(...))`）——否则回写 meta 会让当前这个词从队列消失、直接跳下一个，很跳。复习按钮上分开显示「N 条 · M 词」。
  - **复习记录组件 `ReviewTrack.tsx`（2026-07-28）**：来自 Rosie 自己在 PM 练习里写的用户故事——「我想看到每首诗历次复习的日期，**以便判断是真记牢了、还是每次都在重新背**」。她那条故事挖出的缺口是：历次日期一直存在 `meta.revs` 里，界面上却只显示「第 N/5 次」，**数据有、看不到**。组件把完整曲线摊开成五格：已通过＝实色+日期+「第N次」，今天到期＝描边+「今天」，未到＝虚线+计划日期+「+N天」（2026-07-29 起格子标的是 `ReviewStep.gap`＝**距上次几天**，不再是「学完后第 N 天」——锚点改成相对后那个数跟真实日期对不上了）；点「看明细」列出每次的日期和难度。渲染在三处：复习板块的卡片、`EntryDoc`（古诗）、`ReadingCard`（精读文章）——组件内部用 `isReviewable` 自我判断，不该显示的自己返回 null，调用方不用加条件。
  - ⚠️ **只有日期答不了她那个问题**，所以顺带记了「难度」：`RevRecord = {d, wrong?, rounds?}`——`wrong`＝**第一遍**整篇默写错了几句（后面几轮是订正后复验，不代表记忆强度，所以只取第一遍）、`rounds`＝默了几轮才全对。`ReviewDictation` 通过时回报 `{wrong, rounds}`，一路传到 `passPatch(e, today, stats)`。明细里据此给结论（连着 N 次一遍过＝真记住了／每次都要订正＝还没进长期记忆）。**向后兼容**：`revs` 里老的纯日期字符串和更老的单值 `reviewedAt` 都会被 `reviewLog()` 归一成 `RevRecord`，难度显示「无记录」。**入口＝头部小按钮**（`REVIEW_CFG`，2026-07-21 从大磁贴改成右上角「复习(N)」小按钮，**不进 `BOARDS`**、不占板块磁贴；board==='review' 时 cfg 用 REVIEW_CFG）。**复习专用逐句订正闭环（2026-07-22，`ReviewDictation`，只用在复习板块——当天第一次学仍走 5 遍递进/普通默写）**：不再复用 EntryDoc/ReadingCard 的普通默写，改为①**整篇总体默写**（什么都不看）→②**批改**（见下面 `gradeSents` 铁律）→③**逐句订正**：写错的句子一句一句拎出来（显示正确句，照着重默），**默到全对才进下一句**→④全部订正完**回总体默写复验一轮**(round+1)→⑤总体默写**全对**才「复习通过」。通过即 `onPass`→`patchEntry(id,{reviewedAt:今天})`，头部徽标变「✓已复习」、可「再默一遍」。**复习(N) 计数＝`dueEntries(all).length`**（今天到期/逾期且还没默到全对的条数，默到全对才减一，归零即今日复习完）。复习目标文本 `reviewTarget(e)`：英语=`meta.article_en`‖body，古诗=`meta.recite`‖【原诗】段‖body。
    - ⚠️⚠️ **批改必须「顺序查找」对齐，绝不能按位置对齐**（2026-07-29 修的真 bug，Rosie：「我只错了一句，逐句订正不应该仅订正错的吗，为啥对的也要订正」）。原来是 `splitSents(她写的)[i]` 逐位去比 `sents[i]`——她默《塞下曲》时每个五言句敲了个回车，她那边断出 4 段、原文断出 2 句，**位置整体错开**，于是明明写对的句子也被判错、两句全得订正一遍。**换行、空格、标点写不写，都不该影响判对。** 现在 `gradeSents(answer, sents)`：把她写的整段归一成 token 串（`tokensOf`＝中文按字、英文按词、标点空白全丢），每句原文按顺序在里面 `findSeq`，找到就把游标推到该句之后。于是①格式随便敲；②句内字序仍须完全一致；③句子顺序也仍须对（游标只前进，写颠倒了后一句就找不到）。`sentEq`（单句订正用）也统一走 `tokensOf`，混排中英文不再丢字。
    - ⚠️ `splitSents` **对中文的「，、」也断句**（2026-07-29 加）——古诗一个五言句就是一「句」，这样她只重打真写错的那五个字，不用把整联默一遍。**ASCII 逗号刻意不断**，否则英语精读会被切成一堆从句碎片。
  - **书籍**（`BookBoard`→`BookNotebook`）：书架（封面墙，3:4 海报格）→ 点进「本子」：封面 + 开始/读完日 + 在读/读完切换（status，读完写 meta.finish_date）+ **5 星评分**(`Stars`，存 meta.rating)+ **每日进度/随笔**(kind='note' 的子条目，meta.book_id 关联，按天列)+ **读后感**(body，textarea onBlur 存)。
  - **电影**（`MovieBoard`）：海报卡列表，封面 + 5 星 + 观后感(body)。
  - **产品经理**（2026-07-21 加，第 8 板块，`LearningBoard` 型，kinds＝PM概念/产品拆解/练习；board 值 'pm'，灰色）：为 Rosie 转型 AI PM 最高杠杆的一块——PM 概念卡 + AI 产品拆解（5 问框架）+ 用她自己的 life-dashboard/NOC Sentinel 练"像 PM 地讲"。HCIE **不单开板块**（已在学练计划里，且非转型驱动；网络背景作简历差异化）。
  - **语文/历史内容方向**（Rosie 定位＝课外阅读+找回语文输出/文化感）：语文＝古诗每天背一首(从短到长、先她喜欢的词风，做过词风测试再定)＋练笔输出(治"说话没文化")＋成语配典故；历史＝课外阅读式中国古代史故事，**从诸子百家开篇**(她最感兴趣)再按先秦→往后的时间线走，靠谱来源不戏说。
  - **金融**（2026-07-20 加，第 7 板块，`LearningBoard` 型，kinds＝K线基础/基金知识/基金新闻/我的复盘）：**教育性质**——Claude 教怎么看 K 线/日线、怎么读基金新闻、基金入门概念（指数vs主动/定投/费率/风险）。⚠️ **红线：不做个性化投资建议、不荐具体基金、不给买卖时点**（Claude 非持牌顾问；那类是 Rosie 自己或持牌顾问的决定）。board 值 'finance' 走通用表，无需迁移。
  - ⚠️ 书籍/电影板块**按 `kind !== 'note'` 筛**（不是 `kind==='book'/'movie'`）——旧版建的书/电影 `kind` 是空的，用 kind==='book' 会漏掉（踩过：Rosie 旧版加的《她对此感到厌烦》同步下来后不显示）。笔记子条目才是 `kind='note'`。
  - **封面**：Rosie 报书名/片名 → Claude 联网找封面图 URL 写进 meta.cover（`<img>` 直接显示；没有则占位图标 + "封面待补"提示）。
  - **英语精读系统 v2（方案 A，2026-07-20）**：英语板块用专属 `EnglishBoard`（其余学习板块仍 `LearningBoard`）。`精读文章` 条目用 `ReadingCard`：左＝文章正文，下方「▾展开中文翻译」（整段中文，meta.article_cn）+「默写文章」；右＝**竖排单词本**（meta.words=[{en,cn}]）+「默写单词」。**默写**（`WordDictation`/`ArticleDictation`）：单词=乱序(shuffle)、**先英译中→批改→再中译英→批改→完成本遍**；文章默写＝**5 遍递进 `ReadingDictation`**（2026-07-21）：①看英写中（中文按**字/关键词覆盖率**宽松判，"意思到了就算过"——Rosie 定 B 方案，App 无内嵌模型判不了语义）②看中写英 ③④⑤直接默写英文（按词严格），存 meta.artAtt `slice(-5)`，每遍标 mode(译中/写英/默写)。单词默写仍 3 遍（`WordDictation`）；古诗仍用简单 `ArticleDictation`（3 遍）。批改：中文宽松匹配(cnOk 含子串)、英文规范化全等(enOk)。**默写回顾（2026-07-21）**：批改/切到过往每遍都显示**「你写的」+ 参考原文红标漏错字词**——共享组件 `<DictReview answer refText refLabel>` + `renderMistakes(ref,answer)`（中文按字/英文按词），古诗(ArticleDictation)、英语文章(ReadingDictation)复用；单词(WordDictation)每遍存 `items[{en,cn,a1,ok1,a2,ok2}]`、过往遍逐词显示"你写X应Y"。每遍的答案存进 attempt（ArtAtt 加 `text/ref`、WordAtt 加 `items`），所以三遍能逐遍回看错在哪、不只看分数。⚠️ 回顾只对**改动后新写的**遍生效（旧遍没存 text）；且默写「写新一遍」按钮**不再受 3/5 遍上限隐藏**（写满顶替最旧，`slice(-N)`），否则旧的无-text 记录会挡着、产生不了可回看的新记录。⚠️ **EntryDoc 的「标看完」按钮要调用方传 `onPatch`**（谚语等非默写条目靠它标 meta.done）——EnglishBoard 之前漏传，英语谚语没有标看完，2026-07-21 修。**默写时原文/单词本高糊防偷看**：复用小组件 `<Blurred active>`（`blur-md + select-none + pointer-events-none`）——文章默写糊住文章/学习点/背诵句/中文翻译，单词默写糊住单词本；按钮变「收起默写」退出。⚠️ Claude 写精读时 meta 要带 `article_en / article_cn / words[{en,cn}] / notes / recite`（否则单词本/双译/批改没数据）。写内容用 upsert（`Prefer: resolution=merge-duplicates`）避免覆盖她的默写记录（同一天别重写）。
  - ⚠️ **后台同步不 bump 视图**（App.tsx，2026-07-20）：`key={syncTick}` 只在**首次**同步 bump；聚焦/30s 定时同步只推拉数据、不重挂载——否则会打断默写、把用户弹回板块首页。
  - **卡片配图**（2026-07-20）：`EntryDoc` 若 `meta.image` 存在（data URI）则在正文下渲染 `<img>`。用于金融「K线怎么看」嵌了一张自绘的蜡烛图 SVG（**固定颜色 + 白底自包含**，因为 `<img>` 里的 SVG 拿不到页面 CSS 变量，不能用 var()）。Claude 用 curl 把 svg base64 成 `data:image/svg+xml;base64,…` 塞进 meta.image。
  - **AI 新闻长度定稿**（2026-07-20，Rosie 确认）：每条分【发生了什么 / 为什么值得注意 / 对 AI PM 的意义】三段长版（不是一句话摘要）。金融教育性内容配图 + 通俗讲解 + 真实入门课链接。
  - **下划线术语释义**（2026-07-20）：`EntryDoc` 把 body 里的 `[[术语]]` 渲染成**下划线可点**，点开显示 `meta.glossary`（`{术语: 释义}`）里的解释（浮层框）。用于 AI 新闻里给「多模态 / Sol·Terra·Luna / OpenAI / Z.ai / Meta / EU AI Act」等专业词和公司背景做即点即看的扩充。Claude 写内容时在 body 标 `[[词]]`、并把释义放进 meta.glossary。
  - ⚠️ **含输入的卡片组件全部模块顶层**（Landing/LearningBoard/EntryDoc/AddEntryForm/Stars/BookBoard/BookNotebook/MovieBoard/MovieCard/AddTitleForm），textarea 用非受控 defaultValue+onBlur、其余受控 state 在顶层组件内（避免父组件重渲染失焦）。`patchEntry` 用特殊键 `__body`→body 列、`status`→status 列、其余并进 meta。
  - **内容注入（关键）**：英语/AI 等每日内容 Rosie **不自己贴**——她晚上发 Claude"X 学完了换新的"，**Claude 用 publishable key 经 REST 直接写进她的 `study_entries` 云表**（curl 已验证可写，档位 A anon 全权限），她第二天打开 App 同步下来就有。**前提：Rosie 必须先在 Supabase 建 `study_entries` 表**（否则 404、存不了也同步不了）。生词划线、时间线可视化留 v3。
- **小表格 `mini-table`**：自定义列的轻量表格；**Rosie 把它当周/月复盘中枢**。**版式（2026-07-28 Rosie 要求「表格做大、打开就全屏、外显太不均衡」）**：列表页 `mx-auto max-w-6xl p-6` + 磁贴 `TableTile`（顶层组件，size-11 图标块 + 表名 text-lg + 「N 行 · M 列」+ 绑了数据源的挂「⚡自动填」徽标，行数走 `rowCounts()` 一次聚合查询、别每张表各查一遍）；详情页 `TableDetail` **打开即占满整屏**——根节点 `flex h-full flex-col`，表格区 `min-h-0 flex-1` 自己滚，表头 `sticky top-0` 吸顶。**行要顶满、别留大片空白**（2026-07-28 Rosie 第二轮反馈「还是很空旷，顶满下面，每个框都加长」）：`<Table className="h-full">` 让浏览器把剩余高度按比例摊给各行；单元格里的框跟着长高靠 **`h-px` 老招**——td 给 `h-px`，表格摊完高度后 td 实际高度覆盖这 1px，子元素的 `h-full` 就有了可解析的参照（不加 `h-px` 时 `h-full` 在 td 里解析不出来、框不会长）；框自身再给 `min-h-14` 保底。`cells.tsx` 四种编辑器全部 `h-full min-h-14`（checkbox 那个包裹 div 也要）。行数多到超出高度时 `height:100%` 退化成下限，照旧滚动。⚠️ 吸顶要靠 `[&>[data-slot=table-container]]:h-full/overflow-auto` 把 shadcn `<Table>` 内层那个 `overflow-x-auto` 容器变成真正的滚动容器，否则 sticky 贴在一个永不滚动的盒子上、没效果。单元格 `max-w-72 whitespace-normal break-words`（覆盖 TableCell 默认的 nowrap，靠 twMerge 后写胜出）——否则一格长文本（如「工作」行）会把那一列撑得巨宽、其余六天挤扁。`cells.tsx` 四种编辑器统一 h-10 + text-base。已建表（直接写进云 mini_tables/rows）：**三餐表格**（`tbl-meals-week`，项目+周一~日；行 空腹体重/早餐/午餐/晚餐/总摄入卡路里/睡前体重）、**时间轴周表**（`tbl-plan-week`，2026-07-21；行 养生/英语/工作/学习/运动/阅读）。（原 `tbl-exercise-week` 运动表格 2026-07-21 已软删并入时间轴周表。）**数据源绑定（2026-07-20）**：`sources.ts` 的 `TABLE_SOURCES` 按**表 id**把某些行接到其它模块自动填（不靠行名、不复制数据、每次打开实时算、不改库/不用 SQL）：声明 `itemCol`(标签列)+`dayCols`(mon~sun)+`autoItems`(哪些行自动)+`compute(周日期)→{项目:{列id:值}}`；TableDetail 里自动格只读浅色、其余格手填存 `mini_table_rows`。已绑定：三餐表格（早/午/晚←meal_log 内容、**零食/饮品**←当天 treat_log 的摘要+合计热量（2026-07-28 加，行 id `mrow-meal-snack`、`sort_order=4.5` 插在晚餐与总摄入之间；总摄入那行本来就含它们，这行是拆开看）、总摄入卡路里←dayCalories；**空腹/睡前体重←体重库自动填**，2026-07-22 起不再手填周表——录入口在今日总览）、**时间轴周表**（养生/英语/学习/运动/阅读=当天时间轴里点了已完成的对应 track 条目标题；**日日学**=当天各板块「看完/总数」计数，2026-07-28 加，行 id `mrow-plan-7`、`sort_order=2.5` 排在英语与工作之间（`sort_order` 两侧 schema 都是 REAL，插队直接用小数、不用把后面几行重编号）——条目太多（一天可能十几条）列不下标题，所以给 `英语1/2 语文3/4 AI6/6` 这种紧凑计数，`LOG_BOARDS` 只数六个学习板块、不含书籍/电影；要改成标题就改 `sources.ts` 那一段；**工作**=当天 `done_at` 的 todos 标题）。当前填**本周**（月度复盘待定）；将来「学习表格」加一条绑定即可。**动态卡路里工作流**（Rosie 认可）：她每晚称体重填进三餐表格，Claude 按**早晚体重+趋势**在安全区（约 1420–1550、不低于 BMR 1420）给次日 `app_settings.cal_target` + 饮食建议；运动消耗/摄入由 Claude 估算、Rosie 填表。⚠️ 支持性建议、非医疗处方（同金融红线）。

## 学习记录·每日内容喂养（Claude 每天做的事——换对话/换窗口都照这个走）

学习记录的内容**全部由 Claude 生成后写进 Supabase**（Rosie 不自己贴）。**换新对话/新窗口也要能接着走、绝不重复**——铁律：**先读云端已有的，再续着往下生成**。
**喂养时间（2026-07-28 定）**：Rosie **早上到公司**喊「更新」，正好她去接水冲早餐的几分钟里 Claude 生成 → **`entry_date` 写当天（今天）**，不是次日；这样 AI 新闻也最新。（她没有通勤学英语的环节，所以不需要头天晚上备。）

### 怎么写（curl；publishable key 是公开安全值）
- 表 `study_entries`：`id / board / kind / entry_date(YYYY-MM-DD) / title / body / meta(JSON) / status / sort_order` + 同步字段。
- URL `https://mqzvjrhfvjjuanjkwfsw.supabase.co/rest/v1/study_entries`；Header `apikey:<key>` + `Authorization: Bearer <key>`（key 见 `src/lib/supabase.ts`，公开可用）。
- 写＝POST 一个 JSON 数组，Header 加 `Prefer: resolution=merge-duplicates,return=minimal`（按 id 幂等 upsert，重跑不重复）。
- **id 约定** `<board>-<YYYYMMDD>-<slug/序号>`（如 `chn-20260721-poem1`）。
- ⚠️⚠️ **时间戳铁律（踩过两次坑）**：注入/写云端任何行的 `created_at`/`updated_at` **一律用当前真实 ISO 时间（`date -u`），绝不用固定值/整点/未来时间**（如 `2027-…`、`…T05:00:00Z`）。同步是"最后写入胜出"——一旦注入的时间戳 ≥ 用户之后编辑的时间，用户的编辑（默写记录、体重、笔记）会被这条注入行**盖回丢失**。（这条对 `study_entries`、`mini_table*`、所有表都成立；用户写入侧已加 `updated_at` 单调递增兜底，但注入侧务必守规矩，别再制造未来时间戳。）
- **`entry_date`＝用户查看那天**（她晚上让你备次日内容，就写**次日**日期）——板块默认展开 `entry_date===今天` 那组、没有今天则展开**最新一天**（`LearningBoard`/`EnglishBoard` 的 `primary`）。别用"新闻发生日"当 entry_date，否则次日打开会折叠。
- body 里 `[[术语]]`→下划线点开释义（释义放 `meta.glossary={术语:释义}`）；`meta.image`＝data URI 图，显示在正文下（SVG 要固定颜色+白底，因 `<img>` 读不到页面 CSS 变量）；书/影封面放 `meta.cover`（豆瓣图有防盗链→下载后 base64 成 data URI，别直接存网址）。
- ⚠️ 开发模式默认不联云（见「云端同步方案」），喂内容**用 curl 直接写生产库**，不靠 dev app。

### 不重复的关键：先读后写（进度＝云里已有的）
生成前先 GET 看该 board 已有啥，续着下一条（别重复已讲过的成语/古诗/人物/概念）：
`curl "…/rest/v1/study_entries?board=eq.chinese&select=kind,title,entry_date&order=entry_date.desc" -H "apikey:…" -H "Authorization: Bearer …"`

### 每个板块的每日内容 + 方向（进度指针）
- **英语 english**（kind：精读文章/谚语）：每天一篇 A2 短精读，⚠️⚠️ **篇幅 60–80 词、单词本 6–8 个，硬上限 90 词/10 个**（2026-07-30 收紧，原来写的是 80–120 词）。**这是她自测最吃力的一块**（原话「英语精读对我来说太吃力了」，而语文「背得很快」）——她要求缩短篇幅、**没有要求降低频率**，所以频率不动、只压长度。
  ⚠️ **超标是真实发生过的**：前四篇 71/89/87/78 词、生词 5/9/8/9 个，默写约 7 分钟；**7-29 那篇给到了 169 词 + 17 个生词**，篇幅和词汇量双双翻倍、默写成本涨到 15 分钟——「太吃力」的直接来源就是它。写之前**数一遍词数和生词数**，别凭感觉。
  ⚠️ 为什么长度这么要命：精读要默写 5 遍，**成本跟词数成正比**，而且它是复习队列里最贵的一类（5 轮合计约 24 分钟，占稳态复习成本的一半）。多给 20 个词，后面要还 5 次。
  ⚠️ **生词本里绝不能留省略号或占位符**（2026-08-21 查出：`eng-20260722-reading` 有一条 `the trip takes ...`，原文其实是「The trip takes about forty minutes」）。带 `...` 的条目**学不了也默不了**，而且会进单词表和单词级 SRS 变成一个永远答不对的死条目。写短语就写完整的可记单位（`the trip takes`），要交代用法就写进 `cn` 里。
  meta 必带 `article_en / article_cn(整段翻译) / words[{en,cn}]（生词，含中文释义供默写批改；**要含短语**如 get up/full of，别只给单词）/ notes(学习点) / recite(背诵句)`，再加一句英语谚语。单词本 Rosie **可自己增删**（会的删、缺的加；`WordBook` 组件写回 meta.words）。难度按她水平（≈A2），每周摸排一次再调；晨间另有新概念块，这块保持轻量。
- **语文 chinese**（kind：成语/谚语/古诗/练笔）：每天 **①一个成语（必给意思+出处+例句）②一个谚语/俗语（给意思）③一首古诗背诵**（按词风顺序 **D 边塞 > C 田园 > B 豪放 > E 深情 > F 清新 > A 婉约**，短→长，先在最爱风格里挑、熟了掺别的）**④一个练笔题**（她写、Claude 批改，往"更有画面感/更像人话"改）。历史故事里出现的成语（如韦编三绝）也要补意思。
  - **古诗流派轮换 + 初筛结果（2026-07-27）**：⚠️ **别连着同一派**（曾连给 3 首边塞，Rosie 喊停）——边塞→田园/山水→豪放→婉约→送别→哲理/咏物→思乡…轮着来，短→长。50 首初筛下来 Rosie ＝底子中等偏上 + 一大批"背过已忘"的沉睡存量。策略＝**优先唤醒 🔸 池（读两遍就回来，比学全新快）**、穿插 ❌ 全新；**每 5 首 ≥3 首是她现在背不全的**（池子够大，几乎必然满足），可穿插 1–2 首 ✅ 当热身；唤醒类不强制默写、没时间的天跳过不补；她要多背会说"今天给我两首"。
    - **✅ 已完整会背（避开清单，别再当每日新诗）**：静夜思、春晓、登鹳雀楼、登高、咏鹅、悯农、江雪、寻隐者不遇、游子吟、九月九日忆山东兄弟、赠汪伦、黄鹤楼送孟浩然之广陵、绝句(两个黄鹂鸣翠柳)、枫桥夜泊、夜雨寄北、泊秦淮、清明、如梦令(昨夜雨疏风骤)。
    - **❌ 从没背过（可优先补的全新）**：泊船瓜洲、雨霖铃、乌衣巷、卜算子·咏梅(陆游)、武陵春。
    - **🔸「背过已忘」池（优先唤醒，约 37 首）**：望庐山瀑布/山行/送杜少府之任蜀州/钱塘湖春行/月下独酌/水调歌头/声声慢/山居秋暝/锦瑟/春望/望岳/琵琶行/茅屋为秋风所破歌/相思/鹿柴/竹里馆/过故人庄/长恨歌/无题(相见时难)/赤壁/念奴娇·赤壁怀古/江城子(十年生死)/定风波/一剪梅/青玉案·元夕/破阵子/满江红/饮酒其五/归园田居/观沧海/题西林壁/游山西村/晓出净慈寺送林子方/小池/村居/约客/秋夕。
  - **练笔批改统一格式（2026-07-27 定稿）**：批改**不单开条目**，而是写进**那道练笔题（prompt）条目的 `meta.homework`**（字符串），`entry_date` 跟题目同一天（作业挂在出题那天，别另建新日期）。`EntryDoc` 对练笔条目若 `meta.homework` 存在，就在题目下渲染一个「作业」小框（accent 左色条）。**颜色用 body 行内标记**（`RichText` 组件解析，正文/作业框共用）：`**原文**`＝幼圆加粗（深色）、`!!修改建议!!`＝红字、`!!!示范改写!!!`＝红字加粗、`[[术语]]`＝点开释义；⚠️ **每个标记要在同一行内**（`!!…!!` 里不能有换行，否则不解析）。作业框固定分节（跟 PM 作业格式一致）：**【原文】`**…**` →【批改 · X】→【修改建议】逐条 `!!…!!` →【示范改写】`!!!…!!!` →【一句话心法】`!!…!!`**。
  ⚠️⚠️ **【批改 · X】这一节别再漏**（2026-07-30 补）：X＝字母分数，刻度 **A / A- / B+ / B / B-**（Rosie 2026-07-30 明确要这套字母制），单独一行、夹在【原文】段与下一个小节头之间。**根因值得记住**：07-27 定这套四节格式时**只列了四节、把分数漏了**，于是 07-28 起的作业全都没分（07-21～07-27 每条都有），16 条在 07-30 一次性补齐。规则文件不写，下一个会话还会照着四节漏一次。刻度含义：A＝只有润色级问题／A-＝结论对但有一处实质错误／B+＝方向对但有一处核心概念错误／B＝主要产出得重写／B-＝思路偏了。⚠️ **语文练笔原本不打分**（07-21 起一直没有），07-30 Rosie 说「都给我标进去」后才补上，所以练笔现在也有分。
  原则：先肯定她自己写出的亮点，批改聚焦写作手法（把感受词翻译成看得见的画面），**不越界解读或说教她的私人经历**。（旧做法「单开 `chn-<日期>-writingfb` 批改条目」已废弃并删除，改用本方案。）
- ⚠️⚠️ **生僻字必须注音**（2026-08-17 Rosie 要求，起因是《武陵春》里的「舴艋」没标音、她读不下去）。**对所有板块通用**，古诗和历史最需要——古诗写水景绕不开船具（蓑笠篙橹楫舸）和水生植物（汀渚芷蘅菰蓴蘋），先秦史绕不开礼器（觥簋卣甑醢）、农具（耒耜）、田制（畲圩阡垅畦），这些字成片出现。
  - 做法：正文末尾加一节 **【生僻字注音】**，逐条写「**字（拼音）**——音同某个常用字 + 一句字义」。「音同 X」比只给拼音好用，她能立刻读出来。
  - ⚠️ **也要收「不生僻但意思容易滑过去」的字**：如荀子的「伪」（人＋为＝后天做的，**不是虚伪**）、「拟」（打算）、「堤」（dī 不读 tí）。读错音只是别扭，理解错意思会把整段读反。
  - ⚠️ **注音密度按摸底结果来，别满篇标**：`chn-20260817-hanzi-test` 是 100 字四档的认字摸底（她主动要的），交卷后定一条「注音线」——低于线的标、高于线的不标。**在她交卷之前，宁可多标。**
- **历史 history**（kind：时间线/事件·人物）：课外阅读式中国古代史，**当故事看、不背诵，越全面越好**（Rosie 反馈：一句话/一小段太短，一眼扫完不过瘾）。**每日更新＝一个故事 + 一个人物列传（长版；二者可关联可不关联，Claude 自己定）**。**格式规则**：说人物就讲**完整生平+做的大事**（如孔子：出身/求学/办私学/从政/周游列国/编六经/晚年，1000+字）；说国家讲**国家大事**；说战争讲**主角+时间**；说典故就**详细讲故事**。可讲春秋五霸、竹林七贤、各国局势等专题。**顺序（Rosie 定）**：先秦（**春秋战国**起步，夏商周她也想补）→ 西汉东汉 → 三国 → 隋唐五代十国。诸子百家（孔→孟→老→庄→墨→荀→韩非…）作为春秋战国段的人物线，每位一篇长传。靠谱来源、不戏说演义。
- ⚠️⚠️⚠️ **每天最多一道要动手写的作业**（2026-07-30 定，Rosie：「日日学的任务太繁重了…AI 和 PM 和语文都有输出，比较占时间」）。**输出比阅读慢得多**——她午饭能顺手扫完 3,900 字的历史+金融，但一道要写的思考题能耗掉半小时。原来每天有三四道（语文练笔＋PM概念＋PM产品拆解＋偶尔 AI 题），这是「学不完」的主因，不是阅读量。
  **工作日轮换表**（周末不排作业，她周末在 iPad 上做计划）：

  | 周一 | 周二 | 周三 | 周四 | 周五 |
  |---|---|---|---|---|
  | PM 思考题 | 语文练笔 | PM 思考题 | 语文练笔 | PM 思考题 |

  - PM 的**概念**和**产品拆解**每天照常给内容（她要天天看，那是转型主线），但**只有当天那一个出思考题**，概念/拆解每周轮着当出题的那个。
  - **语文练笔只在周二、周四出**；其余日子语文照常给成语/谚语/古诗，但**不出练笔题**。
  - **AI 练习题只在真碰到值得练的点、且当天那道作业本身很轻时才加**，加了照样写 `exercise: true`。不硬凑。
  - ⚠️ **其余所有条目末尾不再附练习题**。想让她思考可以在正文里写反思式设问（那不算作业），但**不写 `exercise` 标记**——写了就等于又多一道要交的作业。
  - ⚠️ 判断「今天该不该出作业」看的是**星期**，不是「这条内容适不适合出题」。适合出题但不是它的日子，就把那个题留到轮到它的那天。
- ⚠️⚠️ **出了练习题就必须在 meta 里写 `exercise: true`**（2026-07-30 Rosie 要求，原话：「我不要求每一条新闻都向我提出练习题，但我希望提出了练习题的新闻条目不被允许直接标已读，要我给你提交了作业才算已读」）。这条**对所有板块通用**，不只 AI——AI 新闻/术语卡末尾常顺手接一道题（如「这也是你前两天学的『北极星指标』的现成练习题——你会给 XBOW 定哪个北极星？」），语文/金融/历史同理。带了这个标记的条目：`gradedByHomework` 判真 ⇒ **「标看完」按钮消失**、徽标变「交作业后算完成」，她把答案发来、批改写进 `meta.homework` 后才算完成。**没出题就别写这个 key。**
  - 附带好处：给一条**已经被标 done** 的条目补上 `exercise: true`，`entryDone` 会直接忽略 `meta.done`（走 homework 分支）⇒ **自动退回未读**，不用再去清 done 字段。
  - ⚠️ **绝不要改成「检测正文里有没有问号」**来自动判断：①「对 AI PM 的意义」那段满篇是反思式设问（「哪一步在逼用户先打字描述？」），会把大量普通新闻误判成待交作业——2026-07-30 审计时按问号扫 33 条 AI 条目，实测 2 条误报；②这是拿自由文本猜意图，跟「别去解析进度笔记」是同一类错误。
- **AI ai**（kind：新闻/**速览**/术语卡/趋势汇总）：⚠️⚠️ **2026-07-30 减负改版**（Rosie：「日日学的任务太繁重了，学完就没时间工作」）。原来是 5 条三段长版，量出来 **7,407 字/天、占日日学总阅读量 39%**，而真正值得挖「对 AI PM 的意义」的通常只有 1–2 条。现在是：
  - **2 条深读**（kind `新闻`）：仍是【发生了什么 / 为什么值得注意 / 对 AI PM 的意义】三段长版。挑选标准＝**跟她四条线（英语/华为认证/AI/体重）或 AI PM 转型真正相关的**，不是当天最热的。
  - **1 条速览**（kind `速览`，id `ai-<YYYYMMDD>-brief`，标题「速览 · 今日另外 3 条」）：3 条一并放在**同一个条目**里（少三张卡要标已读），每条一个小标题 + 一两句话说清「是什么 + 为什么值得注意」+ 📎 来源链接。⚠️ **速览不写「对 AI PM 的意义」**，那是深读的专属——速览的目的是「知道发生了」，不是「想明白了」。
  - **1 张术语卡 + 1 条趋势汇总**（都保留）。趋势汇总性价比最高，它是把当天串起来的那条；深读+速览一共还是 5 条新闻，所以「连起来看出 2-3 股趋势」照样成立。
  - 目标 ≈ **3,900 字/天**（省一半）。⚠️ 别为了凑字数把速览写成小长版——它超过 200 字就失去意义了。
  专业词/公司名用 `[[ ]]` 标、释义进 meta.glossary。**来源+总结模式（2026-07-27 Rosie 定）**：每条新闻末尾加「📎 来源（点开看原新闻）：URL ｜ URL」（正文里 http(s) 链接会渲染成可点的短链，EntryDoc linkify）——总结是主体、原文自由选看；**每天最后再写 1 条 `趋势汇总`**（把当天 5 条连起来看出 2-3 股趋势 + 一句话结论，晚写使其排在当天最上），不做报告。
- ⚠️⚠️⚠️ **来源链接必须指向「那一条」，不能是聚合站首页**（2026-08-21 Rosie：「这个点进去是固定网址，我没法找到对应哪一条新闻」）。`aiweekly.co/ai-news-today`、`llm-stats.com/ai-news`、`llmgateway.io/timeline` 这类页面**每天换内容**，第二天点进去是别的新闻，等于没给来源。
  - **合格的来源**（按优先级）：①**一手**——arXiv 论文页（`arxiv.org/abs/xxxx`）、厂商官方公告（`openai.com/index/...`）、CVE 详情页、官方 release notes；②**带日期的具体报道**——`thehackernews.com/2026/08/<slug>.html`、`venturebeat.com/...`、Forbes/Axios 的文章页。判据很简单：**这个 URL 一个月后点开还是同一条内容吗？**
  - ⚠️⚠️ **这不只是链接体验问题，是内容质量问题。** 2026-08-21 复核 8/17 那批时查出**三处事实错误 + 一条查不到出处的新闻**（Luna 降价的日期写错、「DeepSeek 涨价 93%」无任何原始出处、ChatGPT 免费档广告写成「即将上线」其实 2 月就开始了、DARPA F-16 那条查不到独立来源已删）。**根因是同一个：当时依赖了聚合站的当日摘要、没回到原始报道去核。** 所以——**链接不可核验，往往说明写的人自己也没核验过。** 先找到一手来源再写，不是写完再补链接。
  - ⚠️ **发现写错了要在条目里显式更正，不要悄悄改掉**：她可能已经读过了，得让她知道哪条不能信。做法是在正文顶部加一段「⚠️ 这条 X 月 X 日更正过」，说清原来错在哪、为什么会错。查不到出处的整条**删掉并留一行说明**，别留着。
- **金融 finance**（kind：K线基础/基金知识/基金新闻/我的复盘）：财商课，教 K线/基金原理/读基金新闻，尽量配图（meta.image）。⚠️ **只教知识，不荐具体基金、不给买卖时点、不做个性化投资建议**（Claude 非持牌顾问）。
- **产品经理 pm**（kind：PM概念/产品拆解/练习）：每天一个 PM 概念卡 + 一个 AI 产品拆解（5 问：用户/痛点/体验/赚钱/我会怎么改）+ 用她的 life-dashboard/NOC Sentinel 练"像 PM 地讲"。
- **书籍 book / 电影 movie**：Rosie 自己录入；Claude 只在她报书名/片名时联网找封面写进 `meta.cover`。

## 云端同步方案（当前优先，2026-07-20 定方向）

目标：跨设备共享同一份数据（家/公司网页/桌面），**不因清浏览器而丢**，撑到 12 月复盘。选型 **Supabase**（Postgres + 免费额度 + 客户端 SDK，静态站点也能用）。

- **需要 Rosie 做的一步**：注册 supabase.com（免费）→ 新建 project → 复制 **Project URL** 和 **anon public key** 给 Claude。anon key **本就是设计成放在前端的公开值**，进仓库/网页都安全——真正的门是数据库的 **RLS（行级安全）策略**，不是藏 key。
- **已落地并建表（2026-07-20）**：Rosie 已在 Supabase 跑 `supabase/schema.sql` 建好 13 张表、验证读写通、云端已清空为干净空库。**首次上线铁律：清空后第一个打开的设备必须是有真实数据的那台**（它当源头推上云），否则空设备的种子会先占位。`src/lib/supabase.ts`（客户端，URL+publishable key，已填 Rosie 的项目 `mqzvjrhfvjjuanjkwfsw`）；`src/lib/sync.ts`（`runSync()`：本地 SQLite/sql.js 仍是工作库，逐表把本地↔云做**全量双向合并**，按 `updated_at` 最后写入胜出、`deleted_at` 当普通字段同步；各表独立 try/catch，未建表/离线只告警不崩）；`App.tsx` 首次进入先同步再渲染（4s 超时兜底，防空库重复播种）、之后聚焦+每 30s 增量同步，拉到新数据用 `key={syncTick}` 强制刷新视图。**Rosie 要做的一次性动作：把 `supabase/schema.sql` 整段粘到 Supabase → SQL Editor 运行**（建 13 张表 + 开 RLS + anon 全权限）。建表后各设备首开会自动把本地数据推上云、别的设备拉下来。⚠️ 改本地表结构时 `supabase/schema.sql` 也要同步加列（第 4 处同步点）。
- 数据源本身**没有**替换（没改各模块的 data.ts / db.ts 读写路径）——sync 是叠加在现有 SQL 数据层之上的旁路，模块代码零改动。
- ⚠️ **开发模式默认不联云**（2026-07-20）：`runSync` 开头 `if (import.meta.env.DEV && localStorage 'pwt-sync-on'!=='1') return false;`——防止本地 `npm run dev` 测试数据污染 Rosie 的生产云（之前踩过：测试浏览器把种子重复推上云）。dev 里要联云手动 `localStorage.setItem('pwt-sync-on','1')`；**生产（网页版/桌面版）永远同步**。
- **Claude 可直接读写云**：用 publishable key `curl`/fetch 到 `…supabase.co/rest/v1/<表>`（档位 A anon 全权限）。已用于给学习记录写每日内容、修数据。⚠️ 批量 DELETE/navigate 会被安全分类器拦（改用 Supabase SQL Editor 让 Rosie 跑）；用非 app 页（如 supabase REST 源）跑只读查询避免触发某个开着 app 的标签页同步。
- ✅ **已修（2026-07-20，原待办 #25）：种子条目重复**。根因：seedIfEmpty / seedHabitsIfEmpty 曾用随机 UUID，两台设备各自播种同一模板→同名不同 id→云端与本地都翻倍。**根治已落地＝种子改确定性 id**：`db.ts` 新增 `seedUuid(key)`（4 轮 FNV-1a 拼 128 位→合法 UUID 格式，同 key 任意设备同 id），`seedIfEmpty` 用 `plan_item:track|title|time_slot`、`seedHabitsIfEmpty` 用 `habit:name` 作 key（均已验证 25+12 条唯一不撞）→ 云同步 upsert 去重。用户手动录入（打卡/笔记/待办）仍用随机 `uuid()`、只创建一次，不受影响。**新增/改动种子条目时保证 key（track+title+time_slot / name）唯一即可**。⚠️ 本修复只防**将来**重复；**已存在的旧重复数据需一次性清理**（数据很年轻，推荐直接 nuke 重播）：Supabase SQL Editor `truncate` 相关表 → 各设备清本地（浏览器清 `pwt-sqljs-db` localStorage / 桌面删 `portwritingtool.db`）→ 只让一台设备先打开重新播种（确定性 id）→ 其余设备再打开自动拉取。
- **隐私档位：已定 A（无登录+公开，2026-07-20 Rosie 拍板）**——anon key + 宽松 RLS，打开网页即见数据、零摩擦。她不介意数据公开；个人小工具、网址无人知，公开写的风险可接受。（备选 B 邮箱登录+每用户 RLS 完全私有，暂不用。）
- **API Key（她最在意）——铁律：个人 API Key 绝不进 GitHub，也不建议「加密后提交」**（公开仓库里的密文可被暴力破解，是坏实践）。正确做法：需要 AI Agent 时，key 在**运行时由她在应用里填入**，只存**她自己浏览器的 localStorage**（每设备填一次、不进仓库/不同步给别人）；或进 Supabase 里**受登录保护的私有行**（选档位 B 时）。更稳可加一个 **serverless 代理**（Supabase Edge Function / Cloudflare Worker）在服务端持 key、前端只调代理——这是要 App 自动调用 AI 时的正解，属二期。

## 部署

- **GitHub**：github.com/BingBing908/life-dashboard（master 分支，公开仓库）
- **网页版**：推送 master 后 GitHub Actions 自动部署到 https://bingbing908.github.io/life-dashboard/（配置 `.github/workflows/deploy-pages.yml`，构建带 `GHPAGES=1` 让 vite base 走 `/life-dashboard/`）
- ⚠️ **CI 报错要能不带 token 看到**（2026-07-28 加）：Actions 的原始日志下载要 token（匿名 403），`check-runs` 的 output 也是空的——没本机 Node 时（换电脑、或 Claude 在没装 Node 的机器上改代码）只能看到 success/failure，等于瞎子。所以 build 步骤改成把报错行 `echo "::error::…"` 转成 **annotation**（`/repos/…/commits/<sha>/check-runs` → `annotations_url` **公开可读**）+ 写进 `$GITHUB_STEP_SUMMARY`。查法：`curl <annotations_url> | grep message`。别把这段删了。
- 网页版数据存各浏览器本地，与桌面端、与其他设备**不互通**（待云同步打通）

## 常用命令

```bash
npm run dev          # 纯浏览器预览（网页开发/无 Rust 环境时用这个，端口 1420）
npm run tauri dev    # 桌面应用开发模式（需要 Rust + VS Build Tools）
npm run build        # 类型检查 + 前端构建（提交前跑一遍）
```

## 路线图

- [x] 核心模块 v1（2026-07）
- [x] 待办改版：四象限筛选 + 今天开关 + 就地编辑 + 每周复盘（2026-07-19）
- [x] UI：蓝白配色 + 幼圆字体（2026-07-19）
- [x] 移除计划表模块（2026-07-19，改用 iPad 规划）
- [x] 学练计划模块：五线周计划 + 每日打卡 + 4 周递进 + 视频跟练链接（2026-07-19）
- [x] 「此刻」时间轴并入学练计划「今天」页（含详细解释、放大、节点间距拉大）+ 删除独立 focus 模块（2026-07-20）
- [x] 打卡添加时可选重复星期（一~日多选，每周重复）（2026-07-20）
- [x] 页面 hash 路由：刷新停在当前页（`#/<module-id>`）（2026-07-20）
- [x] **Supabase 云端同步 + #25 种子重复彻底关闭**（确定性 id + 旧数据一次性清理；push→GitHub Actions 自动部署）（2026-07-20）
- [x] **日日学 v2**（原学习记录）：8 板块（英语/语文/AI/历史/金融/PM/书籍/电影）+ 复习 + 英语精读/单词/古诗默写（2026-07-20~21）

**2026-07-21 本次会话（详情见各模块现状）：**
- [x] **改名**：仪表盘→今日总览、学习记录→日日学、学练计划→时间轴
- [x] **今日总览仪表盘**：各模块今日完成度卡 + 本周时间轴完成柱 + 「本周复盘」→小表格 + 全局经期开关
- [x] **时间轴**：三态打卡按钮(待做/已完成/今天做不了)、每项明细时间、**补卡**(一周视图今天及以前可勾)、工作域「加工作」→建今天·重要紧急待办、计划域「加计划外」、足弓归养生、两个加一行输入框统一到顶
- [x] **待办**：撤「今天的学练计划」镜像(只管工作)、待办↔工作双向、已完成按 `done_at` 分流(今天留列表底/更早进历史已完成、都带「做了什么」)、筛选框↔添加框联动
- [x] **日日学**：每条「今日看完」标记(要默写的默写过才算、其余手动标)、英语精读 **5 遍递进默写**(看英写中→看中写英→纯默×3)、古诗默写、复习改右上角小按钮
- [x] **饮食**：布局重排(三餐左/补剂右上/奶茶右下)、**每日卡路里预算**(BMR 1420/目标/晚餐可吃/基代红线)、补剂只显今天、图标 Pill→Utensils、经期停全部保健品 + 经期饮食建议
- [x] **小表格数据源绑定**：三餐周表 + 时间轴周表(自动填当天完成)，撤运动表并入
- [x] **修复**：笔记输入框失焦(组件提模块顶层)、mini-table/study_entries 未来时间戳吃掉编辑(updated_at 单调递增)
- [x] **去重组件**：`QuickAdd`(加一行) / `DateGroupedBoard`(按日期折叠) / `WeekdayPicker`(星期方块)
- [ ] **视频**：腰突恢复跟练链接待 Rosie 提供（Claude 不编 BV 号；现有个别视频链接可能是早期瞎填的，Rosie 发链接后替换）

**2026-07-22 本次会话：**
- [x] **今日总览·体重趋势**（#2）：体重库 `app_settings weight:<date>:am|pm`（单调递增 updated_at 防护），今日总览录空腹/睡前 + 空腹 SVG 趋势线（目标 58kg 虚线、标最新值与离目标差）；三餐周表空腹/睡前体重行改由体重库**自动填**（不再手填周表，唯一录入口在今日总览），旧手填值已迁入库
- [x] **时间戳铁律固化**（#3）：见下方 ⚠️⚠️ 铁律
- [x] **复习板块逐句订正闭环**（`ReviewDictation`）：整篇默→批改断句→写错的句子逐句订正到全对→全部订正后总体默写复验→**全对才「复习通过」**(reviewedAt=今天)；复习(N) 只数还没默到全对的；判对＝句子词序完全一致（严格档）。仅用在复习板块
- [ ] 月度复盘（小表格目前只填本周）
- [ ] 英语摸底测试；仪表盘卡片拖拽 + 布局持久化；打卡月度热力图 + 番茄钟；UI 美化（先功能后外观）

**2026-07-28 本次会话：**
- [x] **跨多天间隔复习**（原路线图「学习跨多天间隔复习」）：复习板块从「只拉昨天」改成艾宾浩斯间隔（当时是固定的 1/3/7/15/30 天，**2026-07-29 改成相对上次复习的 1/2/4/8/15**），排程逻辑抽到 `study-log/review.ts`；进度存 `meta.revs`、兼容旧 `reviewedAt`；逾期徽标 + 「接下来几天各几条」；一天最多推进一级，攒了不会一次性爆炸
- [x] **小表格加「日日学」行**：`tbl-plan-week` 加 `mrow-plan-7`，自动填当天各板块「看完/总数」
- [x] **小表格版式**：详情页打开即全屏（表头吸顶、长文本换行不撑列）、列表页磁贴放大（行/列数 + 自动填徽标）
- [x] **修「原定计划被当成计划外」**：`isSeedItem` 加内容指纹兜底（种子改过 key 的老行不再显示删除按钮）
- [x] **清掉重复的足弓重建**：Rosie 2026-07-21 误加的那条计划外（`f9bff64c…`，无时段无链接）已软删；留下的 `2ed40c4e…` 是带时段+视频的正主
- [x] **模块内位置全进 URL**（见「路由」铁律）：抽 `lib/hashRoute.ts` 的 `useSubPath`，接上小表格的表、日日学的书本子、时间轴的一周/路线；study-log 原来那套 `boardFromHash` 并进公共实现
- [x] **页面宽度统一全宽**（见「页面宽度约定」）：时间轴/日日学/小表格去掉 max-w-6xl、饮食去掉 max-w-7xl，跟待办和今日总览一致
- [x] **饮食·零食打卡**（复用 treat_log 零改库）+ 右栏四段铺满高度 + 修饮品零食热量没进预算 + 小表格三餐表加「零食/饮品」行
- [x] **总览改版**：改名「总览」、删掉重复的模块入口摘要卡、本周完成柱改按条数画（带刻度/合计）、体重趋势改最近七天、本周复盘直连 `#/mini-table/tbl-plan-week`
- [x] **时间轴改版**：tab 变 **当前 / 今天 / 一周 / 路线**（当前＝A 左栏 + C 右栏三态分栏，今天＝紧凑清单）。详见上面「模块现状」里的说明
**2026-07-29 本次会话：**
- [x] **卡片规格提成全局默认**（见上「卡片规格」）：新增 `src/lib/ui.ts`（CARD/SUBCARD/CARD_BTN/CARD_TITLE/CARD_SUB/PAGE/TWO_COL），六处模块改用常量；日日学的条目卡从 `rounded-lg border bg-background p-3` 升到 CARD（原来 `bg-background` 跟页底同色、卡浮不起来）、小表格磁贴 p-5→p-4、单词复习/电影卡 p-3→p-4
- [x] **两栏布局自适应**：饮食页的内联 `gridTemplateColumns` 换成 `TWO_COL` 断点类，窄屏/放大时塞成一栏
- [x] **待办象限彩签改「点开选」**（原循环点被否，见待办段）
- [x] **饮食页「本周复盘」按钮**：文字居中、删掉「全部自动填好…」那行（Rosie：内部文字布局不好看）
- [x] **复习的四个真 bug**（都是 Rosie 实际用出来的，详见「语文古诗默写 + 复习板块」段）：①批改按位置对齐→写对的句子也要订正（改 `gradeSents` 顺序查找）②艾宾浩斯锚点从 `entry_date` 改成上次实际复习日（`REVIEW_GAPS`，拖延顺延不倒扣，单词 SRS 同步）③默到全对的条目从列表消失（原 `justPassed` 留着是错的）④成语【意思】和【出处】双双泄题（题面遮 `____`、出处不给）
- [ ] 遗留：`2ed40c4e…` 这行 id 仍是老 key 算的（现在靠指纹认作原定计划，功能无碍）。要彻底归位得改 id，但 `plan_checks.item_id` 指着它、打卡历史会断——**不值得动**，除非哪天 resetToSeed 重来一遍

**2026-07-30 本次会话：**
- [x] **修总览「待办」数**（Rosie 截图发现显示 `11/11`）：原来只判 `due_date <= today`，**历史上**标过「今天」又做完的待办永久留在「今天要做」的分母里、还算作已完成，攒了十几条后每天一睁眼就像"全做完了"。照抄时间轴工作域早就在用的谓词（`!t.done || done_at 的日期 === today`），**待办页 / 时间轴工作域 / 总览三处口径现在一致**——总览是最后一个漏了 `done_at` 分流的地方
- [x] **总览顶排按真实使用频率重排**：日日学 → 待办 → 时间轴 → 打卡 → 卡路里（见 PRODUCT.md「总览」段的理由；改顺序只动 `metrics` 数组）
- [x] **文档修一处比代码旧**：「交作业后算完成」的覆盖范围（`gradedByHomework`）已从 `kind==='练笔'` 扩到 PM 的 PM概念/产品拆解/练习，CLAUDE.md 原来只写了练笔
- [x] **PM 作业批改 ×4**（7/28 北极星指标 + Midjourney 我会怎么改、7/29 用户访谈自查 + Notion AI 拆解），写进各题 `meta.homework`。⚠️ 那四条打包的老「练习批改」条目已确认全部软删，不会卡在「交作业后算完成」里拖计数
- [x] **PRODUCT.md 记下〈设计原则 10〉AI 功能的护城河判据**（「完整的每一天」＋「能改明天」，从 7/29 那份 Notion AI 作业里长出来的）。同时**加固「不嵌 AI 模型」这条非目标**——2026-07-30 Rosie 原话「这个平台我大概率不会内嵌 AI，因为你就是最好用的 AI 不是吗」：AI 层已存在（对话里的 Claude 经 REST 读写云库），用那条判据算一遍现在的架构本来就满足，接模型是绕远。**推论（对写代码的人最有用的一句）：App 这侧要守的是「记录完整 + 留好可写回的入口」，任何让记录出窟窿的设计都是在拆护城河**
- ⚠️ **教训（值得记住，它同时是产品结论和工作方法）**：7/29 那份批改第一版**诊断错了**——我从「体重曲线用得少 + 她说做不到六点起」推出「今晨那格绑在她不在电脑前的时刻」，还给了一整套把录入挪到晚上的改法。真实情况是她**出门前称好、到公司记下**，两格设计本来就吃得下，她评价「很好用」。**拿数据配自己的假设去定因果、一句没问用户**——正是那节课讲的陷阱。已更正批改并在 PRODUCT.md 的体重那条写了"别再优化它"。**下次遇到「某功能用得少」，先问再推。**
- [x] **日日学字号调到阅读尺寸**（`READ_BODY` / `READ_TITLE`，见上「密度按用途分」）
- [ ] ⏸ **饮食页压进一屏——挪到有 Node 的家里电脑做**（2026-07-30 Rosie 决定）。**当前状态**：密度已收紧到 `gap-4 / py-4`，但**还长出一节**。已量过：左栏（三餐+本周复盘）够，**瓶颈是右栏**四块＝体重 ~105 / 饮品 ~190 / 零食 ~190 / 月历 ~250 + 三个间距 ~48 ≈ **783px，比可用高度多约 80px**；月历已是 `size-6` 小格子，砍它只有十几像素。**够量的只有一刀＝把「饮品打卡」和「零食打卡」合成一张卡**（用品类标签切换），省 ~96px 正好够——而且它俩本来就共用 `drinkDate` / `DateLine` / `TreatRow` / 同一张 `treat_log`（`kind` 区分），拆成两张卡是「零食后加」的历史原因、不是设计。⚠️ **待 Rosie 拍板**：合并会改变她的操作习惯（记饮品和记零食变成同一表单切品类）。⚠️ **别再凭猜改这一页**——2026-07-30 连着两次没预览就改布局，第二次直接改坏（见上面「包边没了」那条）。这页是全站最挤的一页，改它必须开着 `npm run dev` 看
- [x] **把「统一卡片规格」真正落干净**（Rosie：「可以把这个搞完」）。7/29 那次只落了 `CARD` 和 `TWO_COL`，另四个常量零使用＝半成品。这轮：修正 `PAGE` 的错误定义（`space-y-6 p-6` → `p-6`）并落到六个页面根节点、删掉假抽象 `CARD_SUB`、`SUBCARD` 落 4 处、`CARD_TITLE` 落 9 处、`CARD_BTN` 统一三套可点 hover。详见上面「卡片规格」段。**除 hover 外零外观变化**（只做同值替换，刻意不把 `p-3`→`p-4` 那类"顺手统一"塞进来——那会让饮食页更高，跟她要的反着走）
- [x] **总览底部加「四条线」卡**（原本那片空白）：`components/dashboard/fourLines.ts`（纯计算，**别在组件里重写**）+ `data.ts` 新增 `latestDoneByItem()`／`latestNoteByItem()` + `seed.ts` 新增 `ACCEPTANCE_DATE`／`LINE_TARGETS`（挨着 `SEMESTER_TARGET` 放，防两份漂移）。设计理由与三层数据边界全在 `fourLines.ts` 顶部注释和 PRODUCT.md「总览」段。⚠️ 本周推进天数复用 DashboardShell **本来就有的**「本周完成柱」那个逐天循环（收成 `DaySnapshot[]` 传进去），别为它再查一遍库。⚠️ 体重那格单独走 `buildWeightLine`、在**渲染时**算，好跟着她现填的数字实时变（合成一个函数就得把 weightLog 塞进 effect 依赖，一改数字就重查库）
- [ ] ⚠️ **自检教训（2026-07-30，同一天犯了两次同类错）**：`noUnusedLocals` **不只管 import，也管局部变量**。本机没 Node 时我手写脚本只扫了 import，漏了 `const ids` ⇒ 构建失败。正确的手工自检要**两样都扫**（局部变量用 `grep -oP '^\s*(?:const|let)\s+\K\w+'` 再数出现次数）。CI 的 `annotations_url` 确实能不带 token 读到确切行号，这条路子有效——但它是**事后**发现，别拿它当自检
- [ ] 总览还可以加的（原「底部空白」的其余候选）：**打卡月度热力图**（对应「连续性 > 进度速度」）／「此刻」能直接勾的块。⚠️ 体重**永远别做天级缺口提醒**（她漏了就跳过、不回头补，天天红字只会变成噪音）——这条已落在 `buildWeightLine` 里
- [x] **默写把「文章」和「单词本」拆成两份独立状态**（Rosie：「我默写完单词给文章也标成已默写了…把单词本和文章独立开，仅仅前端显示是叠加在一起的」）：`entryDone` 的 `artAtt || wordAtt` 改成 `dictState(meta).allOk`＝`artOk && (没有单词本 || wordOk)`，三枚徽标各自独立。详见「日日学」段那条 ⚠️⚠️。影响面**只有 `eng-20260720-1` 一条**（文章 0 遍 / 单词 1 遍 ⇒ 正确退回未完成）；云端另 4 条精读两边都是 0，**没有任何「只默了文章」的条目**，所以没人白丢已挣到的完成
- [x] **作业分数补齐 + 写进规则**：`【批改 · X】` 一节（A/A-/B+/B/B-）补进批改格式；云端 16 条带作业的条目全部补上分数（07-28 起漏分的 4 条 pm + 从没打分的 4 条练笔 + 当天新批的 2 条 AI）。漏分的根因＝07-27 定四节格式时没把分数列进去，见批改格式段那条 ⚠️⚠️
- [x] **散步日只排散步**（Rosie：「只要是散步，那么运动页就只允许放散步，不要别的」）：`腰椎稳定` 的 `days` `'*'`→`'1,2,3,5,7'`，`seed.ts` 改好 + 云端那行已 PATCH（id 未变、打卡历史不断、刻意没 bump `SEED_VERSION`）。理由和「以后改不进 id 的字段都这么走」的规矩写在运动那段的 ⚠️⚠️ 里。今天正是周四，运动域从 2 条变 1 条、今日总数 13→12
- [x] **修烟花文案夸大范围**（Rosie：「我的烟花呢，没人给我放烟花耶」引出来的）：烟花一直是**领域级**触发（`study-plan` 的 cheer effect：同一领域待做从 >0 变 0、且无 skip），但文案写的是「今日计划都处理完了 🎉」——她今日 2/12 时就会撞上这句假话。改成 `${active.name}都做完了 🎉`。⚠️ `Fireworks.tsx` 的文件头注释原来也写「今日计划全部完成」，**注释和实现从一开始就不一致**、正是文案写错的源头，已一起改正。Rosie 明确选了「保持领域级，只改文案」，**要改成全天级请连触发条件一起改，别只动文案**。顺带记下：挂载那一刻不放烟花是 `prevPending` 守卫故意的（否则每次打开一个已完成的领域都被炸一次）——所以她今天没被放烟花不是 bug，是最后那条待做被我改 `days` 搬走的、不是她点掉的
- [x] **日日学减负**（Rosie：「日日学的任务太繁重了，我想学完日日学就没时间工作，想工作就学不完日日学」）。**先量后砍**，实测：阅读 18,862 字/天（≈54 分钟）＋复习到期 19 条/68 分钟（**而且九天里只复习过 1 条**，队列从一开始就还不上）＋三四道要写的作业 ⇒ 日成本 2.5–3 小时。
  ⚠️⚠️ **我第一版建议砍错了地方，值得记住**：我按**字数**排成本，推荐把历史（3,911 字）和金融挪到周末、古诗降频。她的回答推翻了它——「历史和金融这种速览，我都是午饭顺手看了不占时间」「我背语文很快，但英语精读对我来说太吃力了」。**字数是坏代理，真实成本是「难度 + 要不要输出」。** 最终她只采纳了两刀，都是关于**形式**而不是**总量**的。
  落地的三项：①**AI 5 条长版 → 2 深读 + 1 条速览(3 则合一) + 术语卡 + 趋势汇总**（7,407→约 3,900 字）②**每天最多一道作业**，一三五 PM／二四练笔的轮换表 ③**精读篇幅 80–120 词收紧到 60–80 词、生词 6–8 个**（7-29 那篇 169 词/17 生词是「太吃力」的直接来源）。
  **未动**：精读/古诗的频率、历史/金融的位置——她明确说不动。
- [ ] **⚠️ 本轮改动未经构建验证**：改动在公司电脑做的，**这台没有 Node**（`D:\Software\DevTools` 不存在，`node` 不在 PATH），跑不了 `npm run build`／`npm run dev`。已按自检教训手工核过「导入 + 局部变量」都无孤儿（新增 `dictState` / `hasWordBook` / `artOk` / `wordOk` / `dictated` 全部有使用点），但**类型检查和真实渲染都没跑过**。推送后去读 CI 的 `annotations_url` 确认，或在有 Node 的机器上先 `npm run build`
- [ ] **番茄钟已砍**（2026-07-30 Rosie：「番茄钟不做，所有电脑现在都有这个功能」）；原路线图「打卡月度热力图 + 番茄钟」只剩热力图那半

## 工作习惯

- **两份文档都随代码同步更新**：改了 `src/` 或 `src-tauri/` 就要把改动反映到本文档（模块现状/路线图/踩坑）；如果改动**推翻或新增了一条产品决策**（不只是实现细节），[PRODUCT.md](PRODUCT.md) 也要改——它是换对话时唯一会被通读的那份，留着旧结论等于误导下一轮。和代码一起提交。由 `.claude/settings.json` 的 Stop hook 自动把关：每轮结束时若发现代码提交比 CLAUDE.md 新，会提醒补文档再收尾（该配置已提交进仓库，各电脑通用）。
- 完成一块有意义的改动就提交并推送（Rosie 多台电脑切换，靠 GitHub 同步代码）
- 改动后习惯用浏览器预览（`npm run dev` + 打开 localhost:1420）验证真实交互再提交
- Rosie 的本机工具链都在 `D:\Software\DevTools`（Node/Rust/VS Build Tools/gh），新开终端可能要刷新 PATH：
  `$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")`
- **本地目录名各台机器不一样**（2026-07-30 更正）：公司这台是 `C:\Users\RosieLi\OneDrive - zenlayer Inc\文档\Claude\life-dashboard`，另一台是 `PortWritingTool`（早期名，SQLite 文件名 `portwritingtool.db` 也是这么来的）。GitHub 仓库名统一是 `life-dashboard`。**别照着文档猜路径，用 `git rev-parse --show-toplevel` 问**
- ⚠️ **公司这台没有 Node**（`D:\Software\DevTools` 只在家里那台）：改完跑不了 `npm run build`／`npm run dev`，纯逻辑改动可以做但必须手工自检导入+局部变量，涉及布局的改动**别在这台改**（见饮食页那条）
