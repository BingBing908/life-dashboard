# life-dashboard 项目全揽（产品文档）

> **这份文档的用途＝压缩上下文。** 换对话、换窗口、换电脑时**先读这一份**，
> 读完就有全貌：产品是什么、有哪些模块、每个决定为什么这么定、代码在哪、当前进展到哪。
>
> **两份文档的分工**（别混着写）：
> | | 这份 `PRODUCT.md` | [`CLAUDE.md`](CLAUDE.md) |
> |---|---|---|
> | 回答 | **是什么 / 为什么** | **怎么改 / 别踩哪个坑** |
> | 体量 | ~20 KB，一次读完 | ~80 KB，按需查 |
> | 读法 | 开工前通读 | 动某个模块前查那一段 |
>
> 所以：**先读这份建立全貌，动手改代码前再查 CLAUDE.md 对应段落。**
> 最后更新：2026-07-29

---

## 30 秒速览

- **是什么**：单人用的生活仪表盘，5 个模块 + 总览，AI 每天往里填学习内容
- **技术**：Tauri 2 + React 19 + TS + Vite / Tailwind v4 + shadcn(Base UI) / SQLite + Supabase 同步
- **规模**：约 8000 行 TS/TSX，最大的两个文件是 `study-log/index.tsx`(1795) 和 `study-plan/index.tsx`(1315)
- **部署**：推 master → GitHub Actions 自动上 Pages；桌面端 Tauri 打包
- **当前阶段**：功能主体已完成，在打磨细节 + 每天喂内容；验收日 2026-12-27
- **最要紧的三条铁律**：注入云端用**真实当前时间戳**（否则盖掉用户编辑）／
  改表结构要**四处同步**／模块内换页状态**必须进 URL**

## 一句话

给一个人用的生活仪表盘：把「今天该做什么、做了没有、学到哪了、吃了多少」收在一个界面里，
并且**由 AI 每天往里填学习内容**，所以它不是一个空壳待办应用。

## 谁在用

Rosie，24 岁，网络工程背景，目标转型 AI 产品经理。使用场景分三处：
工作日在公司用**网页版**，家里用**桌面版**（Tauri），周末的长期规划放 iPad（不在本工具内）。

她的关键特征决定了很多设计取向：

| 特征 | 对产品的影响 |
|---|---|
| **复健期学习者**——曾经能学，中间断了很久 | 用「固定流程 + 进度指针」而不是日历排课。学完一课才进下一课，不赶日期。**连续性 > 进度速度** |
| 同时推四条线（英语 / AI / 华为认证 / 养生运动） | 需要「一屏看完全部」的总览，否则四条线互相遗忘 |
| 腰椎间盘突出、需要规律拉伸和起身 | 计划里有硬性的时间锚点（段间起身 5 分钟、睡前拉伸），不是可选项 |
| 有减脂目标但容易被减脂 App 误导 | 卡路里目标**硬性不低于基础代谢 1420**，低了直接红字警告 |
| 多台设备切换、怕清浏览器丢数据 | 云同步是必需项，不是加分项 |

## 要解决的问题

1. **学过的东西在漏。** 原来学完就学完了，没有回看机制，一周后等于没学。
2. **四条线互相遗忘。** 记在不同地方 = 只有最上面那条活着。
3. **每天要学什么，靠自己想＝不会发生。** 内容准备本身就是最大的摩擦。
4. **减脂靠感觉。** 吃了多少、体重怎么动，没有可对照的记录，就只能焦虑。
5. **数据会丢。** 浏览器一清，几个月的记录归零。

## 明确不做（非目标）

写在这里是为了以后不被"顺手加一下"侵蚀：

- ❌ **不做多用户产品。** 单人工具，隐私档位刻意选了「无登录 + 公开」（见下）。
- ❌ **App 内不嵌 AI 模型。** 热量估算、批改讲解、内容生成都由 Claude 在对话里做，
  结果写进数据库。App 只负责显示和记录。（个人 API Key 绝不进仓库。）
- ❌ **不做投资建议。** 金融板块只教怎么看 K 线、基金原理怎么读，
  **不荐具体基金、不给买卖时点**——Claude 不是持牌顾问。
- ❌ **不做医疗处方。** 饮食和运动建议是支持性的，不是治疗方案。
- ❌ **不做日历式排课。** 见上面「复健期学习者」。
- ❌ **不做花哨的编辑器/富文本。** 内容是喂进来的，不是在里面写作的。

## 成功标准

产品本身没有 DAU 可看（用户只有一个），所以指标挂在**她自己定的验收目标**上，
验收日 **2026-12-27**：

- 体重 ≤ 58kg 达标 / 56.5kg 优秀（55 顺延到 2027 Q1；50 不设为目标）
- 英语：无字幕听懂日常对话 50–60%
- 华为认证：从 HCIA 推进到 HCIP，冲 HCIE
- AI：两个项目（NOC Sentinel、life-dashboard 本身）完善到能写进简历

也就是说：**这个工具的成败 = 这四件事有没有推进**。功能再多，四条线没动就是失败。

---

## 模块

一切功能皆模块（`manifest` + 仪表盘卡片 + 完整页面），加一个模块只需在注册表加一行。
当前 5 个模块 + 1 个总览壳子 + 1 个内嵌面板。

### 总览

进来第一屏。顶部一排今日完成度（时间轴 / 待办 / 卡路里 / 打卡 / 日日学，各自可点进对应模块），
下面是本周时间轴完成柱、最近七天体重趋势、以及全局的**经期开关**。

- 经期开关放在这里是因为它**跨模块**：一开，运动线自动避开腹部动作、补剂全停、
  饮食页出现经期饮食提示（涨的是水不是脂肪，别靠饿减）。放在任何单个模块里都不对。
- 体重录入口只有一个：**前晚睡前 + 今晨空腹**。刻意没有「今晚睡前」——
  她睡前不开电脑，实际都是第二天早上一起填，多一个框只会空着。

### 时间轴（study-plan）

六条线的周计划（养生 / 运动 / 英语 / 华为认证 / AI / 阅读），按她的实际作息带时间段。
四个视图，分工是刻意分开的：

| 视图 | 回答的问题 | 形态 |
|---|---|---|
| **当前** | 此刻该干什么 | 竖线时间轴自动跟随当前时间，右侧是能写笔记的大卡片 |
| **今天** | 今天全天都有什么 | 紧凑清单，一条一行，一屏扫完、随手补勾 |
| **一周** | 这周漏了什么 | 七天网格，今天及以前都能补勾 |
| **路线** | 半年要去哪 | 6 个月的月度目标 + 每周焦点 |

- **打卡是三态不是勾选**：待做 / 已完成 / **未完成**。二态会逼着人把"今天做不了"
  记成"没做"，两者的意义完全不同——前者是决定，后者是遗漏。
- **重要的线要求先写笔记才能打勾**（英语、学习、阅读）：「刷完 001」「看到哪本书哪里」。
  养生和运动不要求——它们做了就是做了，没有进度可记。
- **全部处理完会放烟花**；如果有「未完成」就不放，改给一句鼓励的话。
  奖励要诚实，不然就不是奖励。

### 待办（todo）

只管工作。四象限（重要紧急 / 重要不紧急 / 紧急不重要 / 都不是）+「今天」开关，
筛选框和添加框联动（点哪个象限筛，加新条目就默认那个象限）。

- **和时间轴双向连通**：标了「今天」的待办会出现在时间轴的「工作」线；
  在时间轴加一条工作，就建一条今天·重要紧急的待办。同一份数据，两个入口。
- **已完成按完成日期分流**：今天完成的留在列表底部（还看得见，有成就感），
  更早完成的收进折叠区。
- 右侧内嵌**打卡**周表格（喝水、起身、眼保健操、拉伸、午休…可选重复星期）。

### 饮食（supplement）

- **卡路里预算**：基代 1420 打底，每日目标可调（默认 1400），
  **晚餐可吃 = 目标 − 早 − 午**，实时算。预算条上画着目标线和基代红线，
  目标低于基代直接红字「当心掉发」。
- **三餐 + 补剂 + 饮品 + 零食**四类记录。热量由 Claude 算、她填数字。
- **饮品零食可补记**：点月历某天就记到那天——漏记一天不该就永远补不回来。
- 前晚睡前 / 今晨空腹体重的差 = 一夜的变化，主要是排水，比「今天 vs 昨天同一时刻」
  更能看出前一天吃咸没吃咸。

### 日日学（study-log）

八个板块：英语、语文、AI、历史、金融、产品经理、书籍、电影。
**这是整个工具最独特的部分**——内容不是她自己贴的，是**Claude 每天早上写进云端**的：
她到公司喊一声「更新」，去接水冲早餐的几分钟里内容就生成好了。

每个板块的内容方向都是定过的，不是泛泛地"学点东西"：

- **英语**：一篇 A2 短精读（含整段中译、生词本、背诵句）+ 一句谚语
- **语文**：一个成语 + 一个谚语 + 一首古诗 + **一道练笔题**（她写，Claude 批改）
- **历史**：课外阅读式中国古代史，一个故事 + 一篇人物长传。**当故事看、不背诵**
- **AI**：5 条当天真实新闻（分「发生了什么 / 为什么值得注意 / 对 AI PM 的意义」三段）
  + 1 张术语卡 + 1 条趋势汇总
- **金融**：财商入门，尽量配图
- **产品经理**：一个 PM 概念 + 一个 AI 产品拆解（5 问）+ 拿她自己的项目练"像 PM 地讲"

**「今日看完」的判定是分类型的**，这条很关键：要默写的内容默写过才算看完、
练笔交了作业才算完成、其余手动标。否则「看完」会退化成一个随手点掉的按钮。

#### 复习（间隔重复）

解决「学过的在漏」这个第一号问题。一条内容一共复习 5 次，**每次复习后重置时钟**：
下一次在 1 / 2 / 4 / 8 / 15 天后。五次全对＝毕业，不再出现。

- **锚点是「上次实际复习那天」，不是学完那天。** 这是 2026-07-29 修的设计错误：
  原来锚死学完日，拖延几天再打开，后面几轮全成"已逾期"堆在一起，
  刚默完一条立刻又逾期，永远还不完。现在拖了就顺延、不倒扣。
- **一天最多推进一级。** 长期没打开时，队列不会把五轮全怼到脸上。
- **默到全对就从列表消失。** 列表是待办，做完就该清掉。
- 两种复习形式：**默写型**（古诗、精读、谚语）＝整篇默 → 批改 → 把写错的句子逐句订正到全对
  → 再整篇默一遍全对；**回想型**（成语）＝看意思答成语。
- **成语的方向刻意是反的**（给意思、答成语）：她学成语是为了"治说话没文化"，
  真实场景是*想表达某个意思时能不能调出那个成语*。反过来只练认得出，练不出用得出。
- **加复习类型前先算稳态负担**：间隔 5 段 ⇒ 每天到期条数 ≈ 5 × 每天新进条数。
  所以贵的（全文默写 8–9 分钟）要克制、便宜的（一句谚语 20 秒）可以多加。
  精读从第 3 轮起降级成只默背诵句，就是为了腾出空间装谚语、成语和单词。
- **单词是按词调度，不是按篇。** 同一篇里有的词一遍就记住、有的反复错，
  按篇复习会让会的词陪着不会的词一起重复。

**一条真实的用户故事 → 功能的例子**（她自己在 PM 练习里写的）：

> 「我想看到每首诗历次复习的日期，**以便判断是真记牢了、还是每次都在重新背**。」

这条故事挖出的缺口是：历次日期一直存在数据库里，界面上却只显示「第 N/5 次」——
**数据有、看不到**。于是有了「复习曲线」组件：五格摊开，已通过的显示日期，
点开明细还能看每次的难度（一遍过 / 错几句几轮），并据此给结论
（「连着 3 次一遍过——这首是真记住了」）。

### 小表格（mini-table）

自定义列的轻量表格，她当**周复盘中枢**用。当前两张：三餐周表、时间轴周表。

- **关键设计：部分行自动填。** 体重、三餐内容、总摄入、各线完成情况都从别的模块实时算，
  不复制数据、不用手抄。手填的格子和自动格颜色不同。
- 打开即全屏、表头吸顶、行铺满高度——它是用来"盯着看一周走势"的，不是零星查一格。

---

---

## 代码在哪（文件地图）

一个模块自包含在 `src/modules/<名字>/`（`index.tsx` 页面 + `data.ts` 读写），
在 `registry.ts` 注册一行即上架。**不要把功能硬编码进仪表盘壳子。**

```
src/
  App.tsx                     hash 路由（#/<模块id>），首次同步再渲染
  components/
    dashboard/DashboardShell   总览（唯一允许跨模块读数据的地方）
    ui/                        shadcn 组件（Base UI 版：trigger 用 render 不是 asChild）
    EditableText / Collapse / DoneToggle / QuickAdd / Fireworks   通用件，重复即抽这里
  lib/
    db.ts        运行环境检测：Tauri→plugin-sql / 浏览器→sql.js+localStorage
    schema.ts    浏览器端建表 + 补列迁移
    sync.ts      Supabase 全量双向合并（last-write-wins on updated_at）
    supabase.ts  URL + publishable key（公开值，可进仓库）
    hashRoute.ts useSubPath()——模块内换页状态进 URL 的公共实现
    ui.ts        全局卡片规格 CARD / SUBCARD / TWO_COL（改一次大小只动这里）
    dates.ts     日期工具（todayStr/addDays/mondayOf/daysBetween/formatDateCn）
  modules/
    study-plan/  时间轴：当前/今天/一周/路线 四视图 + seed.ts 六线周计划模板
    todo/        待办四象限 + WeeklyReview 每周复盘
    supplement/  饮食：卡路里预算 + 三餐/补剂/饮品/零食 + 体重
    study-log/   日日学八板块 + 复习（review.ts 排程 / ReviewDictation /
                 ReviewQuiz / ReviewTrack / wordReview.ts 单词级 SRS）
    habit-checkin/ 打卡面板（未单独注册，内嵌在待办页右栏）
    mini-table/  小表格 + sources.ts（哪些行自动填、从哪算）
src-tauri/src/lib.rs           桌面端 SQLite 迁移（当前 v12）
supabase/schema.sql            云端建表 + RLS（Rosie 在 SQL Editor 跑）
.github/workflows/deploy-pages.yml  自动部署；build 失败会输出 annotation
```

## 数据表（14 张，全部带 id/created_at/updated_at/device_id/deleted_at 软删除）

| 表 | 装什么 |
|---|---|
| `plan_items` / `plan_checks` / `plan_notes` | 六线计划条目 / 每日三态打卡 / 每日进度笔记 |
| `todos` | 待办（象限、due_date、done_at） |
| `habits` / `habit_checkins` | 习惯（含重复星期）/ 每日打卡 |
| `meal_log` / `treat_log` | 三餐（date+meal 唯一）/ 饮品**和零食**（`kind` 区分） |
| `study_entries` | 日日学全部内容，一张通用表（board/kind/entry_date/title/body/meta JSON） |
| `mini_tables` / `mini_table_rows` | 小表格定义 / 行数据 |
| `app_settings` | 键值杂物：卡路里目标、经期开关、体重库 `weight:<date>:am\|pm`、种子版本 |
| `study_sessions` / `study_subjects` | 旧版学习记录遗留，日日学 v2 已不用 |

**复习进度不在单独的表里**——存在 `study_entries.meta` 的 `revs`（条目）
和 `wordSrs`（每个单词）里。刻意的：零改库。

## 铁律速查（细节看 CLAUDE.md 对应段）

1. **时间戳**：往云端写任何行，`created_at`/`updated_at` 必须是**真实当前 ISO 时间**。
   用固定值或未来时间 ⇒ 同步的"最后写入胜出"会把她之后的编辑（默写记录、体重、笔记）盖掉。踩过两次。
2. **改表结构四处同步**：`lib.rs` 迁移 + `schema.ts` 建表 + `schema.ts` 浏览器补列 + `supabase/schema.sql`。
   新表还要进 `sync.ts` 的 `TABLES`。
3. **模块内换页状态进 URL**：用 `useSubPath`，别只放组件 state，否则刷新就被弹回首页。
4. **含 `<input>` 的组件必须定义在模块顶层**，不能嵌在页面组件函数体内——
   否则父组件每次 setState 都让它重新挂载，输入框失焦（"只能打一个字"）。
5. **页面宽度＝全宽 `p-6`，不加 `max-w`**；卡片一律用 `lib/ui.ts` 的 `CARD`。
6. **粗体保持幼圆，不要黑体。**
7. **种子条目用确定性 id**（`seedUuid(key)`），否则多设备各自播种会翻倍。
8. **内容注入前先 GET 云端已有的**，续着往下生成，别重复讲过的成语/古诗/人物。
9. **`noUnusedLocals` 是开的**：删代码留下的孤儿 import/组件/常量会让构建失败。
10. **本机没 Node 时靠 CI 验证**：推上去读 check-runs 的 `annotations_url`（公开可读，不需要 token）。

## 数据与同步

- **本地优先**：SQLite 是工作库（桌面端是文件，网页端是 sql.js + localStorage），
  离线可用。
- **Supabase 做双向全量合并**，按 `updated_at` 最后写入胜出，软删除也同步。
  首次进入先同步再渲染，之后聚焦时 + 每 30 秒增量同步。
- **隐私档位刻意选了 A：无登录 + 公开。** 打开网页即见数据，零摩擦。
  她本人拍板："个人小工具、网址无人知，公开写的风险可接受。"
  （备选 B＝邮箱登录 + 每用户隔离，随时可切，代价是每台设备都要登录。）
- **个人 API Key 绝不进仓库**，包括"加密后提交"——公开仓库里的密文可被暴力破解。
  真要 App 自己调 AI，正解是加一层服务端代理，属二期。

## 部署

- 网页版：推送即自动部署到 GitHub Pages
- 桌面版：Tauri 打包
- 内容注入：Claude 直接写云端库，她第二天打开就同步下来

## 路线图

**已完成**（详细清单见 CLAUDE.md 的路线图段）

核心五模块 · 云同步 · 间隔复习 · 英语精读默写系统 · 卡路里预算 ·
小表格数据源自动填 · 时间轴四视图改版 · 全局卡片规格统一

**接下来**

- 月度复盘（小表格目前只覆盖本周）
- 英语摸底测试（现在是按 A2 估的，需要定期重测校准难度）
- 打卡月度热力图 + 番茄钟
- 仪表盘卡片拖拽 + 布局持久化
- 腰突恢复跟练视频链接（等她提供真实链接——**不编 BV 号**）

**想知道"最近改了什么"**：`git log --oneline -20`，或看 CLAUDE.md 路线图段——
那里按会话日期列了每次做完的事。这份文档只记稳定的结论，不记流水。

## 每天要做的事（换对话也照这个走）

Rosie 早上到公司喊一声「更新」，Claude 在她接水冲早餐的几分钟里：

1. **先 GET 云端** `study_entries` 看各板块已有什么（不重复讲过的内容）
2. 按上面「日日学」各板块的方向生成当天内容，`entry_date` **写当天**
3. POST 上去（`Prefer: resolution=merge-duplicates`，按 id 幂等），
   id 约定 `<board>-<YYYYMMDD>-<slug>`
4. 时间戳用真实当前时间（见铁律 1）

她另外会随时发来：练笔作业要批改、报书名要找封面、报吃了什么要算热量、
体重数据要给次日卡路里目标建议。

## 设计原则（从踩过的坑里长出来的）

1. **一个东西只有一个录入口。** 体重只在总览填、笔记只在时间轴写，
   别处只读或自动同步。两个地方能填同一个数，就一定会对不上。
2. **列表是待办，做完就清掉。** 留着"已完成"占位看着热闹，实际看不出还剩几条。
3. **撑满高度时，让一个块吸收剩余空间，不要平摊给所有块。**
   平摊的结果是每张卡中间空一块，比留白更难看。
4. **模块内会「换页」的状态一律进 URL。** 刷新、误触后退、从别处跳回来都不该被弹回首页。
5. **奖励要诚实。** 全部做完才放烟花，有未完成就只给鼓励话。
6. **判对判错要宽容格式、严格内容。** 换行、空格、标点写不写都不该影响判对；
   字序、拼写必须对。
7. **提示要够认出来，但不能给出答案。** 复习古诗给标题作者、谚语给中文；
   成语的题面里出现答案的地方要遮掉——包括释义里自带的那处。
